---
title: "SpecFirst: Behavioral Specification Elicitation as a First-Class Step in Agent-Based Program Synthesis from Scratch"
title_zh: SpecFirst：将行为规范提取作为基于智能体的从零开始程序合成中的一等步骤
authors: "Yihao Chen, Shi Chang, Feng Lin, Khaled Chawa, Boyuan Chen, Shaowei Wang, Ahmed E. Hassan"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.27167v1"
tags: ["query:re-us"]
score: 8.0
evidence: 受经典需求工程启发的行为规范提取
tldr: 针对大模型在从零构建程序时表现不佳的问题，本文提出 SpecFirst 框架。该框架受需求工程启发，将行为规范提取作为独立阶段，先由专门代理通过探测二进制文件和阅读文档生成结构化规范，再由代码代理进行合成。实验表明，该方法在 ProgramBench 上显著提升了测试通过率和探索覆盖率，证明了显式需求分析在程序合成中的重要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27167v1/fig-001.webp\", \"caption\": \"Fig. 1: Framework of SPECFIRST. On top of the default code synthesis pipeline in the blue box, SPECFIRST introducesspec agent to elicit the specification before implementation in the green box.\", \"page\": 4, \"index\": 1, \"width\": 1045, \"height\": 433}]"
motivation: 现有 LLM 代理在从零合成程序时，往往将文档阅读、行为探索和代码编写混为一谈，导致对复杂行为理解不足且错误累积。
method: 提出 SpecFirst 两阶段框架，首先通过 Spec 代理探测二进制文件并结合文档生成结构化规范，随后由代码合成代理基于该规范完成实现。
result: "在 ProgramBench 的 200 个实例上，SpecFirst 将测试通过率提高了 6.9%-21.3%，二进制探索覆盖率提升了 9.4%-18.5%。"
conclusion: 将行为规范提取作为程序合成的独立首要步骤，能有效解决文档歧义并提供稳定的行为参考，是提升从零构建程序能力的有效范式。
---

## 摘要
基于大语言模型（LLM）的智能体在已有代码库提供上下文的软件工程任务中表现出色，但从零开始构建程序在根本上仍然更具挑战性。最近的基准测试（如 ProgramBench）量化了这一差距：在仅提供自然语言文档和作为行为先知（behavioral oracle）的仅可执行二进制文件的情况下，即使是前沿模型也只能解决不到 1% 的实例。现有框架将文档阅读、行为探索和代码合成混为一谈，合并在单次流程中，导致智能体探测不足、随着上下文偏移而丢失行为意图，并将早期的误解传播到最终实现中。受经典需求工程的启发，我们认为行为规范提取应该是一个先于实现的一等阶段（first-class phase）。我们提出了 SpecFirst，这是一个两阶段框架，强制在代码合成之前进行规范提取。一个专门的规范智能体首先探测二进制文件，并将观察结果与文档结合成结构化的规范。随后，代码合成智能体利用该规范来驱动实现。这种分解在编码开始前解决了文档的歧义，并在整个合成过程中提供了稳定的行为参考。我们在所有 200 个 ProgramBench 实例上评估了 SpecFirst，涵盖了两个系列、能力跨度达一个数量级的四个模型。SpecFirst 一致优于单循环基线，将测试通过率提高了 6.9%-21.3%，二进制探索覆盖率提高了 9.4%-18.5%，且均具有统计学显著性。对代码合成的行为分析进一步表明，预先制定的规范能够实现更早且更持续的代码构建。我们的结果表明，显式的需求工程阶段是从零开始构建程序的有效范式。

## Abstract
LLM-based agents excel at software engineering tasks where an existing codebase provides context, but constructing a program from scratch remains fundamentally harder. Recent benchmarks such as ProgramBench quantify this gap: given only natural-language documentation and an execute-only binary as a behavioral oracle, even frontier models solve fewer than 1% of instances. Existing frameworks conflate documentation reading, behavioral exploration, and code synthesis into a single pass, causing agents to probe insufficiently, lose behavioral intent as context drifts, and propagate early misinterpretations into the final implementation. Inspired by classical requirements engineering, we argue that behavioral specification elicitation should be a first-class phase that precedes implementation. We present SpecFirst, a two-stage framework that forces the specification elicitation before code synthesis. A dedicated spec agent first probes the binary and combines observations with documentation into a structured specification. Next, a code synthesis agent then uses this specification to drive implementation. This decomposition resolves documentation ambiguities before coding begins and provides a stable behavioral reference throughout synthesis. We evaluate SpecFirst on all 200 ProgramBench instances across four models spanning two families and an order of magnitude of capability. SpecFirst consistently outperforms the single-loop baseline, improving test pass rates by 6.9%-21.3% and binary exploration coverage by 9.4%-18.5%, all statistically significant. Behavioral analysis on code synthesis further shows that a prior specification enables earlier and more sustained code construction. Our results demonstrate that an explicit requirements-engineering phase is an effective paradigm for from-scratch program construction.

---

## 论文详细总结（自动生成）

这是一份关于论文《SpecFirst: Behavioral Specification Elicitation as a First-Class Step in Agent-Based Program Synthesis from Scratch》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：LLM 智能体在“从零开始”构建程序（From-scratch Program Synthesis）时表现极差。即使是 GPT-5.5 等前沿模型，在 ProgramBench 基准测试中的完全解决率也低于 1%。
*   **研究背景**：现有的智能体框架（如 SWE-agent、OpenHands）通常将文档阅读、行为探测（Probing）和代码编写混杂在同一个循环中。
*   **痛点分析**：这种“混杂模式”导致了三个系统性问题：
    1.  **探测不足**：智能体在未充分理解程序行为前就急于编码。
    2.  **规范丢失**：随着对话轮次增加，长上下文导致最初获取的行为意图发生偏移或被遗忘。
    3.  **错误传播**：早期的误解没有稳定的参考物来纠正，导致错误在后续步骤中不断累积。

### 2. 方法论：SpecFirst 框架
*   **核心思想**：受传统软件工程中“需求工程”阶段的启发，将**行为规范提取（Specification Elicitation）**作为一个独立且首要的阶段，与代码合成阶段解耦。
*   **关键流程**：
    1.  **第一阶段：规范智能体（Spec Agent）**：
        *   输入：自然语言文档（README/Man page）和仅可执行的二进制文件（作为行为先知）。
        *   动作：通过黑盒探测（边界值测试、错误路径触发、组合标志测试、输出格式精化）系统地探索二进制文件的行为。
        *   输出：生成一个结构化的 Markdown 文件（`SPEC.md`），包含概述、标志位、输入/输出格式、错误模式和边缘案例。
    2.  **第二阶段：代码合成智能体（Code Synthesis Agent）**：
        *   输入：原始文档、二进制文件以及第一阶段生成的 `SPEC.md`。
        *   动作：将 `SPEC.md` 作为稳定的行为契约，专注于代码实现。
*   **约束机制**：严禁智能体通过反汇编、源码恢复或网络搜索等手段“作弊”，确保所有知识均来自黑盒观察。

### 3. 实验设计
*   **数据集/场景**：使用 **ProgramBench** 全量 200 个实例。这些实例涵盖了 FFmpeg、SQLite、PHP 解释器等真实世界的复杂命令行程序，涉及 Rust、Go、C/C++ 等多种语言。
*   **对比方法（Baselines）**：
    *   **Direct-Synthesis**：原始的 ProgramBench 流程（即单循环模式，使用 mini-SWE-agent 框架）。
*   **评估模型**：涵盖了 Qwen3.5-397B、Qwen3.6-35B、GPT-5.5-high 和 GPT-5.4-mini 四款模型，跨越了不同的能力等级。
*   **评估指标**：平均测试通过率（Test Pass Rate）和探测覆盖率（Probing Coverage，通过插桩二进制文件测量）。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长，因为该研究主要基于现有的预训练模型 API 进行推理实验。
*   **成本分析**：文中详细列出了推理成本。SpecFirst 的平均单实例成本比基线高出 48%–130%（例如 GPT-5.5-high 约为 5.85 美元/实例），这主要是由于增加了专门的规范提取阶段。

### 5. 实验数量与充分性
*   **实验规模**：在 200 个任务实例上对 4 种模型进行了完整测试，总计进行了数百次长程智能体运行。
*   **充分性**：
    *   **统计显著性**：使用了 Wilcoxon 符号秩检验（p < 0.05）验证改进的显著性。
    *   **消融/对比实验**：对比了不同的规范格式（自由格式、OpenSpec 格式、结构化标题格式）。
    *   **行为分析**：深入分析了智能体在不同阶段的 Token 分配和代码增长曲线。
*   **客观性**：实验设置了严格的防作弊检测，确保结果反映的是智能体的推理和探索能力。

### 6. 主要结论与发现
*   **性能提升**：SpecFirst 在所有模型上均显著优于基线，测试通过率提升了 **6.9%–21.3%**。
*   **覆盖率增加**：专门的规范阶段使二进制行为探测覆盖率提升了 **9.4%–18.5%**。
*   **行为转变**：有了预先制定的规范后，代码合成智能体能够更早地开始编码，且代码构建过程更持续，减少了在编码阶段因不确定性而进行的盲目探测。
*   **质量分布**：SpecFirst 显著增加了“近乎完美”（通过率 > 90%）实现的比例（例如 GPT-5.5 从 5.5% 提升至 16.5%）。

### 7. 优点
*   **范式创新**：首次将需求工程作为智能体程序合成的一等公民，解决了长程任务中的上下文漂移问题。
*   **解耦设计**：通过外部化的 `SPEC.md` 充当“知识锚点”，有效缓解了 LLM 在复杂逻辑下的认知负担。
*   **通用性强**：实验证明该方法在不同规模、不同家族的模型上均有效。

### 8. 不足与局限
*   **执行故障（F4 模式）**：即使规范描述正确，仍有 52% 的失败源于实现阶段的逻辑错误，说明仅靠规范提取无法解决 LLM 所有的编码缺陷。
*   **成本开销**：由于引入了额外的推理阶段，经济成本和时间成本显著增加。
*   **应用限制**：目前仅在确定性的命令行程序上验证，对于具有非确定性、图形界面或复杂进程间通信的程序，其探测策略可能失效。
*   **环境依赖**：约 8% 的失败源于基准测试环境的噪声（如硬编码的路径或超时），而非智能体本身的能力。

（完）
