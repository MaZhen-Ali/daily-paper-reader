---
title: "SpecFirst: Behavioral Specification Elicitation as a First-Class Step in Agent-Based Program Synthesis from Scratch"
title_zh: SpecFirst：将行为规范提取作为基于智能体的从零开始程序合成的一等步骤
authors: "Yihao Chen, Shi Chang, Feng Lin, Khaled Chawa, Boyuan Chen, Shaowei Wang, Ahmed E. Hassan"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.27167v1"
tags: ["query:re-us"]
score: 8.0
evidence: 受经典需求工程启发进行行为规范提取
tldr: 针对大语言模型在从零构建程序时面临的文档理解与行为探索难题，本文提出 SpecFirst 框架。该框架借鉴需求工程思想，将行为规范提取作为独立阶段，先由专门代理通过探测二进制文件和阅读文档生成结构化规范，再由合成代理据此编写代码。实验表明，该方法在 ProgramBench 上显著提升了测试通过率和探索覆盖率，证明了显式需求工程在程序合成中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27167v1/fig-001.webp\", \"caption\": \"Fig. 1: Framework of SPECFIRST. On top of the default code synthesis pipeline in the blue box, SPECFIRST introducesspec agent to elicit the specification before implementation in the green box.\", \"page\": 4, \"index\": 1, \"width\": 1045, \"height\": 433}]"
motivation: 现有框架将文档阅读、行为探索与代码合成混为一谈，导致模型在从零构建程序时容易产生误解并缺乏充分的行为探测。
method: 提出 SpecFirst 两阶段框架，先由规范代理生成结构化行为规范，再由合成代理基于该规范驱动代码实现。
result: "在 ProgramBench 上的评估显示，SpecFirst 将测试通过率提升了 6.9%-21.3%，并将二进制探索覆盖率提高了 9.4%-18.5%。"
conclusion: 将行为规范提取作为程序合成的首要独立阶段，能有效解决文档歧义并提供稳定的行为参考，是提升从零构建程序能力的有效范式。
---

## 摘要
基于大语言模型（LLM）的智能体在已有代码库提供上下文的软件工程任务中表现出色，但从零开始构建程序仍然具有根本性的难度。最近的基准测试（如 ProgramBench）量化了这一差距：在仅提供自然语言文档和仅可执行二进制文件作为行为预言机（oracle）的情况下，即使是前沿模型也只能解决不到 1% 的实例。现有框架将文档阅读、行为探索和代码合成混为一谈，导致智能体探测不足、随着上下文漂移而丢失行为意图，并将早期的误解传播到最终实现中。受经典需求工程的启发，我们认为行为规范提取应该是先于实现的一等阶段。我们提出了 SpecFirst，这是一个两阶段框架，强制在代码合成之前进行规范提取。一个专门的规范智能体首先探测二进制文件，并将观察结果与文档结合成结构化的规范。随后，代码合成智能体利用该规范来驱动实现。这种分解在编码开始前解决了文档歧义，并在整个合成过程中提供了稳定的行为参考。我们在跨越两个系列、能力跨度达一个数量级的四个模型上，针对所有 200 个 ProgramBench 实例评估了 SpecFirst。SpecFirst 一贯优于单循环基线，测试通过率提高了 6.9%-21.3%，二进制探索覆盖率提高了 9.4%-18.5%，且均具有统计学显著性。对代码合成的行为分析进一步表明，预先制定的规范能够实现更早且更持续的代码构建。我们的结果表明，显式的需求工程阶段是从零开始构建程序的有效范式。

## Abstract
LLM-based agents excel at software engineering tasks where an existing codebase provides context, but constructing a program from scratch remains fundamentally harder. Recent benchmarks such as ProgramBench quantify this gap: given only natural-language documentation and an execute-only binary as a behavioral oracle, even frontier models solve fewer than 1% of instances. Existing frameworks conflate documentation reading, behavioral exploration, and code synthesis into a single pass, causing agents to probe insufficiently, lose behavioral intent as context drifts, and propagate early misinterpretations into the final implementation. Inspired by classical requirements engineering, we argue that behavioral specification elicitation should be a first-class phase that precedes implementation. We present SpecFirst, a two-stage framework that forces the specification elicitation before code synthesis. A dedicated spec agent first probes the binary and combines observations with documentation into a structured specification. Next, a code synthesis agent then uses this specification to drive implementation. This decomposition resolves documentation ambiguities before coding begins and provides a stable behavioral reference throughout synthesis. We evaluate SpecFirst on all 200 ProgramBench instances across four models spanning two families and an order of magnitude of capability. SpecFirst consistently outperforms the single-loop baseline, improving test pass rates by 6.9%-21.3% and binary exploration coverage by 9.4%-18.5%, all statistically significant. Behavioral analysis on code synthesis further shows that a prior specification enables earlier and more sustained code construction. Our results demonstrate that an explicit requirements-engineering phase is an effective paradigm for from-scratch program construction.

---

## 论文详细总结（自动生成）

这是一份关于论文《SpecFirst: Behavioral Specification Elicitation as a First-Class Step in Agent-Based Program Synthesis from Scratch》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管 LLM 智能体在修复 Bug 或代码补全方面表现出色，但“从零开始”构建完整程序仍然极具挑战。在 ProgramBench 等基准测试中，即使是顶尖模型在仅有文档和二进制文件的情况下，成功率也低于 1%。
*   **核心问题**：现有框架将“阅读文档”、“行为探测”和“代码合成”混杂在同一个迭代循环中。这导致了三个系统性缺陷：
    1.  **探测不足**：智能体在未充分理解行为前就急于编码。
    2.  **规范丢失**：随着对话轮次增加，长上下文漂移导致模型遗忘最初的行为意图。
    3.  **错误传播**：早期的误解没有外部锚点进行纠正，导致错误不断累积。
*   **整体含义**：论文借鉴经典软件工程中的“需求工程”思想，主张将**行为规范提取（Specification Elicitation）**作为一个独立且首要的阶段，先于代码合成执行。

### 2. 方法论
*   **核心思想**：提出 **SpecFirst** 框架，将任务分解为两个解耦的阶段：
    *   **第一阶段：规范提取（Spec Agent）**：专门的智能体通过黑盒探测（输入参数、stdin、观察 stdout/stderr/退出码）和阅读文档，系统性地挖掘程序行为，并输出一个结构化的 Markdown 规范文件（`SPEC.md`）。
    *   **第二阶段：代码合成（Code Synthesis Agent）**：合成智能体以 `SPEC.md` 为行为契约，专注于代码实现。
*   **关键技术细节**：
    *   **探测模式**：包括边界探测、错误路径触发、组合标志测试和输出格式精化。
    *   **约束机制**：严禁智能体通过反编译、源码恢复等手段“作弊”，强制其仅通过黑盒交互获取知识。
    *   **结构化输出**：规范文件包含六个固定部分：概述、标志位、输入/标准输入、输出格式、错误模式、边缘案例。
    *   **上下文管理**：在阶段转换时压缩信息，仅传递 `SPEC.md` 而非冗长的探测日志，防止上下文过载。

### 3. 实验设计
*   **数据集**：使用 **ProgramBench** 全量套件，包含 200 个真实的开源命令行程序实例（涉及 Rust, Go, C/C++, Java, Haskell 等语言）。
*   **任务难度**：涵盖 Easy (28), Medium (143), Hard (29) 三个等级。
*   **对比基线**：**Direct-Synthesis**（直接合成模式），即使用相同的智能体架构（如 mini-SWE-agent）但不经过独立的规范提取阶段。
*   **评估模型**：跨越两个系列、四种规模的模型：Qwen3.5-397B-A17B、Qwen3.6-35B-A3B、GPT-5.5-high、GPT-5.4-mini。
*   **评价指标**：平均测试通过率（Pass Rate）和探测覆盖率（Probing Coverage）。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的本地 GPU 硬件配置（如 A100 数量等），因为实验主要通过 API 调用（LiteLLM 代理）完成。
*   **环境配置**：所有实验在隔离的 Docker 容器中运行，每个任务设有 6 小时墙钟时间限制和 1000 轮次的步数限制。
*   **成本分析**：详细记录了推理成本（USD），SpecFirst 的平均成本比基线高出 48% 至 130%，主要源于额外的规范提取阶段。

### 5. 实验数量与充分性
*   **实验规模**：在 200 个实例上对 4 种模型进行了完整测试，总计进行了数百组端到端运行。
*   **充分性与公平性**：
    *   使用了统计显著性检验（Wilcoxon 符号秩检验，p < 0.05）。
    *   通过消融实验验证了不同规范格式（Freeform vs. Sections vs. OpenSpec）的影响。
    *   分析了代码行数随时间的变化曲线，以证明性能提升源于行为理解的改善而非单纯的算力堆叠。
    *   实验设计客观，通过禁止源码搜索等手段确保了黑盒探测的真实性。

### 6. 论文的主要结论与发现
*   **性能提升**：SpecFirst 在所有模型上均显著优于基线，测试通过率提升了 **6.9%–21.3%**。
*   **覆盖率增加**：探测覆盖率提升了 **9.4%–18.5%**，证明 Spec Agent 能更有效地挖掘隐藏行为。
*   **行为转变**：有了预先定义的规范，代码合成智能体能够**更早地开始编码**，且生成的代码库规模更大、更完整，减少了在编码阶段的盲目试错。
*   **难度适应性**：在 Hard 难度任务中，GPT-5.5-high 的表现提升了 29.9%，说明该框架对复杂逻辑的拆解尤为有效。

### 7. 优点
*   **架构解耦**：成功将复杂任务分解为“理解”与“执行”两个清晰的阶段，符合人类软件工程的最佳实践。
*   **缓解长上下文问题**：通过外部化的 `SPEC.md` 充当“持久化记忆”，有效防止了模型在长程任务中的意图漂移。
*   **通用性强**：该方法不依赖于特定模型架构，在开源和闭源模型上均表现出一致的增益。

### 8. 不足与局限
*   **成本开销**：引入额外的 Spec Agent 显著增加了推理成本和总运行时间。
*   **执行层瓶颈**：失败案例分析显示，52% 的错误仍源于“执行故障”（即规范正确但代码实现错误），这表明仅靠规范提取无法解决所有合成难题。
*   **场景局限**：目前仅针对确定性的命令行程序（CLI），对于具有非确定性、图形界面（GUI）或复杂网络交互的程序，黑盒探测的难度将大幅增加。
*   **环境依赖**：约 8% 的失败源于基准测试环境的配置问题，而非智能体本身的能力。

（完）
