---
title: "SpecFirst: Behavioral Specification Elicitation as a First-Class Step in Agent-Based Program Synthesis from Scratch"
title_zh: SpecFirst：将行为规范提取作为基于智能体的从零开始程序合成的一等步骤
authors: "Yihao Chen, Shi Chang, Feng Lin, Khaled Chawa, Boyuan Chen, Shaowei Wang, Ahmed E. Hassan"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.27167v1"
tags: ["query:re-us"]
score: 8.0
evidence: 受经典需求工程和行为规范提取的启发
tldr: 针对大模型在从零开始构建程序时面临的文档理解不足和行为探索不充分等问题，本文提出了 SpecFirst 框架。该框架借鉴经典需求工程思想，将行为规范提取作为独立阶段。通过专门的 Spec 智能体先探测二进制程序并结合文档生成结构化规范，再由代码合成智能体进行实现。实验表明，SpecFirst 在 ProgramBench 上的测试通过率和探索覆盖率均显著提升，证明了显式需求工程在程序合成中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27167v1/fig-001.webp\", \"caption\": \"Fig. 1: Framework of SPECFIRST. On top of the default code synthesis pipeline in the blue box, SPECFIRST introducesspec agent to elicit the specification before implementation in the green box.\", \"page\": 4, \"index\": 1, \"width\": 1045, \"height\": 433}]"
motivation: 现有框架将文档阅读、行为探索和代码合成混为一谈，导致模型在从零构建程序时容易产生误解并难以维持行为意图。
method: 提出两阶段框架 SpecFirst，先由 Spec 智能体生成结构化行为规范，再由代码合成智能体基于该规范驱动实现。
result: "在 ProgramBench 上的评估显示，该方法使测试通过率提升了 6.9%-21.3%，二进制探索覆盖率提升了 9.4%-18.5%。"
conclusion: 将行为规范提取作为程序合成的首要独立步骤，能有效解决文档歧义并为代码构建提供稳定的行为参考。
---

## 摘要
基于大语言模型（LLM）的智能体在已有代码库提供上下文的软件工程任务中表现出色，但从零开始构建程序在根本上仍然更加困难。最近的基准测试（如 ProgramBench）量化了这一差距：在仅提供自然语言文档和仅可执行二进制文件作为行为预言机（behavioral oracle）的情况下，即使是前沿模型也只能解决不到 1% 的实例。现有框架将文档阅读、行为探索和代码合成合并为单一过程，导致智能体探测不足、随着上下文漂移而丢失行为意图，并将早期的误解传播到最终实现中。受经典需求工程的启发，我们认为行为规范提取应该是一个先于实现的一等阶段。我们提出了 SpecFirst，这是一个两阶段框架，强制在代码合成之前进行规范提取。一个专门的规范智能体首先探测二进制文件，并将观察结果与文档结合成结构化的规范。接着，代码合成智能体使用该规范来驱动实现。这种分解在编码开始前解决了文档歧义，并在整个合成过程中提供了稳定的行为参考。我们在所有 200 个 ProgramBench 实例上评估了 SpecFirst，涵盖了两个家族的四个模型，其能力跨度达一个数量级。SpecFirst 一贯优于单循环基线，将测试通过率提高了 6.9%-21.3%，二进制探索覆盖率提高了 9.4%-18.5%，且均具有统计学显著性。对代码合成的行为分析进一步表明，预先的规范能够实现更早且更持续的代码构建。我们的结果表明，显式的需求工程阶段是从零开始构建程序的有效范式。

## Abstract
LLM-based agents excel at software engineering tasks where an existing codebase provides context, but constructing a program from scratch remains fundamentally harder. Recent benchmarks such as ProgramBench quantify this gap: given only natural-language documentation and an execute-only binary as a behavioral oracle, even frontier models solve fewer than 1% of instances. Existing frameworks conflate documentation reading, behavioral exploration, and code synthesis into a single pass, causing agents to probe insufficiently, lose behavioral intent as context drifts, and propagate early misinterpretations into the final implementation. Inspired by classical requirements engineering, we argue that behavioral specification elicitation should be a first-class phase that precedes implementation. We present SpecFirst, a two-stage framework that forces the specification elicitation before code synthesis. A dedicated spec agent first probes the binary and combines observations with documentation into a structured specification. Next, a code synthesis agent then uses this specification to drive implementation. This decomposition resolves documentation ambiguities before coding begins and provides a stable behavioral reference throughout synthesis. We evaluate SpecFirst on all 200 ProgramBench instances across four models spanning two families and an order of magnitude of capability. SpecFirst consistently outperforms the single-loop baseline, improving test pass rates by 6.9%-21.3% and binary exploration coverage by 9.4%-18.5%, all statistically significant. Behavioral analysis on code synthesis further shows that a prior specification enables earlier and more sustained code construction. Our results demonstrate that an explicit requirements-engineering phase is an effective paradigm for from-scratch program construction.

---

## 论文详细总结（自动生成）

这是一份关于论文《SpecFirst: Behavioral Specification Elicitation as a First-Class Step in Agent-Based Program Synthesis from Scratch》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**从零开始构建程序（From-scratch Program Construction）**这一极具挑战性的任务。
*   **研究背景**：虽然 LLM 智能体在修复 Bug 或添加功能等任务上表现出色，但在没有任何代码库参考、仅有自然语言文档和可执行二进制文件（作为行为参考）的情况下，即使是最强模型（如 GPT-5.5）在 ProgramBench 基准测试中的完全解决率也低于 1%。
*   **核心问题**：现有框架将文档阅读、行为探测和代码编写混杂在同一个循环中，导致三个系统性缺陷：
    1.  **探测不充分**：智能体在未完全理解行为前就急于编码。
    2.  **规范丢失**：随着对话轮次增加，长上下文导致模型遗忘最初发现的行为细节。
    3.  **错误传播**：早期的误解没有稳定的参考物来纠正，导致错误不断累积。

### 2. 方法论：SpecFirst 框架
受经典软件工程中“需求工程”阶段的启发，论文提出了 **SpecFirst**，将行为规范提取（Elicitation）作为独立的一等公民阶段。
*   **核心思想**：将任务分解为两个阶段，强制在编写代码前先生成一份详尽的结构化规范文档（`SPEC.md`）。
*   **关键流程**：
    1.  **规范提取阶段（Spec Agent）**：专门的智能体通过黑盒探测（输入参数、Flag、stdin）与二进制文件交互，结合文档，针对边界情况、错误路径、Flag 组合和输出格式进行系统性探索。
    2.  **结构化产出**：生成包含概览、Flags、输入/输出格式、错误模式和边界情况的 Markdown 文档。
    3.  **代码合成阶段（Code Synthesis Agent）**：接收原始文档、二进制文件以及上阶段生成的 `SPEC.md`，专注于实现。
*   **约束机制**：禁止智能体通过反汇编、源码恢复或网络搜索等“作弊”手段获取信息，确保其完全通过黑盒观察提取规范。

### 3. 实验设计
*   **数据集/基准测试**：使用 **ProgramBench**，包含 200 个真实的开源命令行程序（涵盖 C/C++、Rust、Go 等语言），如 FFmpeg、SQLite 等。
*   **对比方法**：
    *   **基线（Direct-Synthesis）**：使用 mini-SWE-agent 框架，在单一循环中直接进行合成。
    *   **实验模型**：涵盖 Qwen3.5-397B、Qwen3.6-35B、GPT-5.5-high 和 GPT-5.4-mini 四种不同规模和家族的模型。
*   **评估指标**：平均测试通过率（Pass Rate）和探测覆盖率（Probing Coverage）。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **推理成本**：论文详细记录了推理成本（API 调用费用）。SpecFirst 的平均单实例成本比基线高出 48%–130%（例如 GPT-5.5-high 约为 5.85 美元/实例），这主要是由于增加了专门的规范提取阶段以及生成了更复杂的代码。

### 5. 实验数量与充分性
*   **实验规模**：在全部 200 个 ProgramBench 实例上进行了测试，并跨越了 4 个不同能力的模型。
*   **充分性与公平性**：
    *   进行了**统计显著性检验**（Wilcoxon 符号秩检验，p < 0.05）。
    *   进行了**消融实验**（对比不同规范格式：自由格式、OpenSpec、结构化分段）。
    *   进行了**行为分析**，量化了代码行数随时间的变化。
    *   实验设计客观，通过禁止“作弊”行为确保了对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：SpecFirst 在所有模型上均显著优于基线，测试通过率提升了 **6.9%–21.3%**。
*   **探索深度**：Spec 智能体将二进制文件的代码覆盖率提升了 **9.4%–18.5%**，证明了专门阶段对理解复杂行为的必要性。
*   **行为转变**：有了预先规范后，代码合成智能体会更早开始编写代码，且生成的代码库规模更大、更完整，而不是在探测和编码之间反复横跳。
*   **质量分布**：SpecFirst 显著增加了“近乎完美”（通过率 > 90%）的程序比例。

### 7. 优点
*   **范式创新**：将传统的需求工程思想成功引入智能体编程领域，解决了长上下文下的意图漂移问题。
*   **解耦设计**：通过外部化的 `SPEC.md` 充当“锚点”，缓解了模型在长程任务中的认知负担。
*   **鲁棒性**：实验证明该方法在不同模型家族和不同难度（Easy/Medium/Hard）的任务下均有效。

### 8. 不足与局限
*   **成本增加**：由于增加了额外的推理步骤，经济成本和时间开销明显高于单循环方案。
*   **实现阶段仍有瓶颈**：失败案例分析显示，52% 的失败源于“执行故障”（即规范写对了但代码没写对），说明仅靠规范提取无法解决模型逻辑推理能力的底层限制。
*   **环境依赖**：部分失败源于环境配置（如网络字符串、超时等），这属于基准测试本身的噪声。
*   **适用范围**：目前仅在确定性的命令行程序上验证，对于图形界面（GUI）或非确定性系统的适用性尚待研究。

（完）
