---
title: Towards an Agentic LLM-based Approach to Requirement Formalization from Unstructured Specifications
title_zh: 迈向基于智能体大语言模型的非结构化规范需求形式化方法
authors: "Alberto Tagliaferro, Bruno Guindani, Livia Lestingi, Matteo Rossi"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18228v1"
tags: ["query:re-us"]
score: 9.0
evidence: 从非结构化规范中提取需求并形式化
tldr: "针对安全关键系统早期规范多为非结构化自然语言、难以直接进行形式化验证的问题，本文提出一种基于智能体（Agentic）的LLM方法。该方法通过包含需求提取、兼容性过滤及形式化翻译的模块化流水线，自动将非结构化规范转化为语义对齐的形式化属性。实验表明，该方法在保证语法正确性的同时，显著提升了语义一致性，准确率达77.8%，为弥合非正式描述与形式化验证之间的鸿沟提供了新路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18228v1/fig-001.webp\", \"caption\": \"Table 2: Distribution of requirement extraction accuracy across the evaluated scenarios.\", \"page\": 8, \"index\": 1, \"width\": 853, \"height\": 562}]"
motivation: 现有的LLM生成形式化属性方法往往只关注语法正确性，而忽视了与原始非结构化需求之间的语义对齐，导致难以直接用于安全验证。
method: 提出一种模块化的智能体流水线，整合了需求提取、针对目标形式化语言的兼容性过滤以及最终的形式化属性翻译。
result: "在三种不同场景下的实验结果显示，该方法生成的属性在语法和语义上均表现良好，综合准确率达到77.8%。"
conclusion: 该研究通过显式考虑建模与验证约束，证明了利用AI自动化弥合非正式规范与语义化形式验证之间差距的可行性。
---

## 摘要
安全关键系统的早期规范通常以自然语言表达，这使得推导适用于验证且保证安全所需的形式化属性变得困难。虽然近期基于大语言模型（LLM）的方法可以从文本生成形式化产物，但它们主要关注语法正确性，而无法确保非正式需求与可形式化验证属性之间的语义一致性。我们提出了一种基于智能体的方法论，能够从非结构化规范中自动提取待验证属性。该模块化流水线结合了需求提取、针对目标形式系统的兼容性过滤以及向形式化属性的转换。在三个场景下的实验结果表明，该流水线生成的语法和语义一致的形式化属性准确率达到 77.8%。通过明确考虑建模和验证约束，该方法为利用人工智能（AI）弥合非正式描述与具有语义意义的形式化验证之间的鸿沟奠定了基础。

## Abstract
Early-stage specifications of safety-critical systems are typically expressed in natural language, making it difficult to derive formal properties suitable for verification and needed to guarantee safety. While recent Large Language Model (LLM)-based approaches can generate formal artifacts from text, they mainly focus on syntactic correctness and do not ensure semantic alignment between informal requirements and formally verifiable properties. We propose an agentic methodology that automatically extracts verification-ready properties from unstructured specifications. The modular pipeline combines requirement extraction, compatibility filtering with respect to a target formalism, and translation into formal properties. Experimental results across three scenarios show that the pipeline generates syntactically and semantically aligned formal properties with a 77.8% accuracy. By explicitly accounting for modeling and verification constraints, the approach is a paving step towards exploiting Artificial Intelligence (AI) to bridge the gap between informal descriptions and semantically meaningful formal verification.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards an Agentic LLM-based Approach to Requirement Formalization from Unstructured Specifications》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在安全关键系统（如人机交互机器人）的早期开发阶段，需求通常以非结构化的自然语言（NL）描述。为了进行形式化验证以确保安全性，必须将这些模糊的文本转化为精确的形式化属性（如时序逻辑）。
*   **核心挑战**：现有的 LLM 辅助方法虽能生成语法正确的代码或公式，但往往缺乏**语义对齐**（即生成的公式是否真实反映了原始意图）以及**可验证性**（生成的属性是否符合目标模型的边界和约束）。
*   **研究目标**：提出一种基于智能体（Agentic）的流水线，自动从非结构化规范中提取既符合语法又具备语义一致性、且可直接用于工具验证的形式化属性。

### 2. 方法论：核心思想与关键流程
论文提出了一种由三个专门化阶段组成的模块化智能体流水线：
1.  **阶段 1：需求提取（Requirements Extraction）**
    *   利用 LLM 将非结构化文本分解为原子化的自然语言需求（JSON 格式），并进行标准化处理（如“系统必须...”）。
2.  **阶段 2：可验证性分类（Verifiability Classification）**
    *   引入一个“法官”智能体，根据目标形式化模型（如随机混合自动机 SHA）的语义边界和观测变量，过滤掉无法验证的需求（如涉及不可观测变量或主观设计意图的需求）。
3.  **阶段 3：形式化翻译（Formal Translation）**
    *   将筛选后的需求翻译为目标形式化语言（本研究中为 UPPAAL SMC 查询语句）。通过在 Prompt 中嵌入 BNF 范式确保语法正确，并利用 LLM 处理自然语言与形式化逻辑之间的语义映射。

### 3. 实验设计
*   **实验场景**：使用了三个典型的网络实体系统（CPS）场景：咖啡递送（SC1）、用户引导运输（SC2）和工厂流水线（SC3）。
*   **基准测试（Benchmark）**：作者手动构建了地面真值（Ground Truth, GT），包含 71 条自然语言需求和 72 条对应的形式化查询。
*   **评估方法**：
    *   **LLM-as-a-Judge**：由于形式化表达具有多样性（如逻辑等价性），采用 LLM 作为裁判来评估生成结果与 GT 之间的语义一致性。
    *   **语法检查**：使用 UPPAAL 的 `verifyta` 编译器进行强制性语法验证。

### 4. 资源与算力
*   **模型型号**：实验主要使用 `gemini-1.5-pro-preview`（注：原文中提及为 gemini-3.1-pro-preview，可能为未来版本号或笔误，通常指 Gemini 系列模型）。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方法基于现成的 LLM API 进行推理（Prompt Engineering），不涉及模型训练，因此算力消耗主要集中在 API 调用上。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 个复杂场景进行了端到端的测试，涵盖了从提取到翻译的全流程。
*   **充分性评价**：
    *   **客观性**：通过引入编译器检查和双重 LLM 裁判机制，减少了主观偏见。
    *   **局限性**：实验场景数量较少（3 个），且主要集中在人机交互（HRI）领域。虽然展示了消融式的阶段评估（RQ1-RQ3），但缺乏与其他 SOTA（先进技术）工具的直接对比实验。

### 6. 主要结论与发现
*   **高准确率**：最终生成的验证查询中，**95.8% 满足语法正确性**，经过语义对齐评估后的**有效准确率达到 77.8%**。
*   **过滤的重要性**：阶段 2 的分类器达到了 88.7% 的准确率，有效防止了不可验证的需求进入翻译阶段，减少了计算浪费。
*   **语义等价性**：实验发现，严格的字符串匹配会严重低估 LLM 的表现（仅 34.7%），而 LLM 能够识别逻辑等价（如 $A \land B$ 与 $B \land A$）的变体。

### 7. 优点与亮点
*   **模块化设计**：将复杂任务分解，提高了可解释性和可追踪性，方便开发者在任何阶段进行人工干预。
*   **领域无关性**：虽然实验在 HRI 领域，但流水线逻辑（提取-过滤-翻译）可扩展至其他形式化框架。
*   **语义对齐**：不仅关注“能运行”，更关注“翻译得准”，通过 LLM 裁判解决了形式化表达多样性的评估难题。

### 8. 不足与局限
*   **逻辑严谨性缺失**：LLM 在提取需求时有时会遗漏严格的逻辑边界（如防止死循环的条件）或幻觉出常识性约束。
*   **规模限制**：目前的评估集较小，对于超大规模、跨多个文档的复杂规范，其上下文处理能力和一致性维护尚待验证。
*   **错误传播**：虽然有过滤机制，但第一阶段提取的错误仍可能影响后续步骤。

（完）
