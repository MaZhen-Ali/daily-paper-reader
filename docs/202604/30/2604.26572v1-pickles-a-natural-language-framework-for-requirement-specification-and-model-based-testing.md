---
title: "PICKLES: a Natural Language Framework for Requirement Specification and Model-Based Testing"
title_zh: PICKLES：一种用于需求规范和基于模型测试的自然语言框架
authors: "María Belén Rodríguez, Petra van den Bos"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26572v1"
tags: ["query:re-us"]
score: 8.0
evidence: 用于需求规范和BDD场景的自然语言框架
tldr: 本研究结合了基于模型测试（MBT）和行为驱动开发（BDD）的优势，提出了PICKLES框架。该框架通过扩展Gherkin风格的结构化自然语言，实现了人类可读的需求规范与形式化模型之间的双向转换。通过将多个场景组合成主模型并利用MBT算法自动生成测试用例，PICKLES在保持易读性的同时，显著提升了测试覆盖率，并在工业级交通管理系统案例中验证了其有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26572v1/fig-001.webp\", \"caption\": \"Figure 3 Smaster. Each line style (dashed, dotted, solid, dash-dotted) denotes transitions from S1–S4. Variables follow the reference in Table 1.\", \"page\": 10, \"index\": 1, \"width\": 1021, \"height\": 549}, {\"url\": \"assets/figures/arxiv/2604.26572v1/fig-002.webp\", \"caption\": \"Figure 2 STS1: Faulty detectors outside the critical section.\", \"page\": 8, \"index\": 2, \"width\": 520, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.26572v1/fig-003.webp\", \"caption\": \"Figure 4 Coverage of STSmaster. Transitions and states covered by only executing the specification scenarios are shown in black, whereas transitions in red show non-tested transitions.\", \"page\": 12, \"index\": 3, \"width\": 509, \"height\": 367}]"
motivation: 旨在解决BDD缺乏形式化模型支持和MBT规范难以被人类理解的问题，实现易读性与自动化测试生成的结合。
method: 提出了一种名为PICKLES的结构化自然语言，并开发了将其转换为形式化模型及组合成主模型的双向翻译机制。
result: 在工业案例研究中，PICKLES在相同场景集下实现了比传统BDD显著更高的测试覆盖率。
conclusion: PICKLES成功弥合了自然语言规范与自动化模型测试之间的鸿沟，为复杂系统的需求验证提供了高效且可读的解决方案。
---

## 摘要
本文结合了基于模型测试（MBT）和行为驱动开发（BDD）领域的方法，定义了一种测试方法，该方法既具有类似 BDD 的人类可读规范和测试用例，又利用了来自 MBT 的建模技术和自动测试生成算法。我们引入了 PICKLES（一种用于测试场景的精确输入和控制流关键字语言），它是对 Gherkin 风格 BDD 场景的扩展，采用结构化自然语言进行规范。我们提供了从 Pickles 场景到形式化模型的双向转换，其中规范和测试均具有人类可读性，并提供了一种获取所谓“主模型”（master model）的方法，该模型结合了所有转换后的场景。随后可以应用标准的 MBT 算法从中自动导出测试用例。我们实现了转换和组合步骤的原型，并将其应用于一个工业案例研究：交通管理系统中的一个软件组件。通过该案例，我们展示了整个流程，并证明了 Pickles 如何在相同场景集下实现比 BDD 显著更高的覆盖率。

## Abstract
This paper combines methods from the fields of Model-Based Testing (MBT) and Behaviour-Driven Development (BDD) to define a testing approach with human-readable specifications and test cases, as in BDD, while using the modelling techniques and automatic test generation algorithms from MBT. We introduce PICKLES, a Precise Input and Control-flow Keyword-based Language for tEst Scenarios; an extension of Gherkin-style BDD scenarios, specified in structured natural language. We provide a bi-directional translation from Pickles scenarios to formal models, where both specifications and tests are human-readable, and a method to obtain a so-called master model combining all translated scenarios. Standard MBT algorithms can then be applied to automatically derive test cases from it. We implement a prototype of the translation and composition steps, which we use on an industrial case study: a software component from a traffic management system. With it, we illustrate the pipeline and show how Pickles can achieve significantly higher coverage with respect to BDD from the same set of scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《PICKLES: a Natural Language Framework for Requirement Specification and Model-Based Testing》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对软件测试中**行为驱动开发（BDD）**与**基于模型测试（MBT）**之间的脱节问题提出了解决方案。
*   **背景**：BDD（如 Gherkin 语法）因其自然语言的易读性在工业界广受欢迎，但其基于示例的特性导致测试覆盖率有限且难以维护。MBT 虽然能通过形式化模型自动生成高覆盖率的测试，但建模门槛高，非技术利益相关者难以理解。
*   **核心问题**：如何在保留 BDD 人类可读性的同时，引入 MBT 的严谨性和自动化测试生成能力？

### 2. 方法论：核心思想与关键技术
论文提出了 **PICKLES** 框架，其核心流程如下：
*   **PicklesDSL**：对 Gherkin 语法的扩展。它保留了 `Given-When-Then` 结构，但增加了“变量设置块”（Variable Settings Block），用于显式定义变量类型（原始类型、数组、结构体）、取值范围和约束条件。
*   **双向转换机制**：
    *   **正向**：将 Pickles 场景自动转换为**符号转换系统（Symbolic Transition Systems, STS）**。每个场景映射为一个部分模型。
    *   **逆向**：将 MBT 算法生成的形式化测试用例转换回人类可读的 Pickles 格式。
*   **模型组合（Master Model）**：
    *   使用**选择组合（Choice Composition, $\biguplus$）**合并初始状态相同的场景。
    *   使用**顺序组合（Sequential Composition, $\triangleright$）**将场景串联，模拟系统在长时间运行中的连续行为。
    *   最终生成一个“主模型”，代表系统的完整预期行为空间。
*   **自动化测试生成**：利用现有的 MBT 算法（如基于覆盖率的路径搜索）从主模型中导出测试路径，并通过 SMT 求解器确定具体的输入参数值。

### 3. 实验设计
*   **实验场景**：使用荷兰 Technolution B.V. 公司的一个真实工业案例——**交通管理系统组件**。该组件负责处理道路传感器的遥测数据，识别关键路段的故障并更新 UI 状态。
*   **Benchmark 与对比方法**：
    *   **对比对象**：传统的 BDD 方法（即手动编写的、孤立执行的 Gherkin 场景）。
    *   **评估指标**：转换覆盖率（Transition Coverage）和输入空间覆盖率（Input Coverage）。
*   **实验流程**：将原始的 BDD 测试用例改写为 Pickles 规范，生成主模型，随后对比 Pickles 自动生成的测试与原始手动测试的覆盖范围。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件配置（如 GPU 型号或 CPU 核心数）。
*   **实现细节**：作者开发了一个基于 Python 的原型工具，使用了 `Lark` 库进行语法解析。由于该方法主要涉及符号逻辑运算和 SMT 求解（用于参数选择），对算力的需求远低于深度学习模型，通常在普通工作站即可运行。

### 5. 实验数量与充分性
*   **实验规模**：论文主要围绕**一个深入的工业案例研究**展开，包含 4 个核心业务场景。
*   **充分性评价**：
    *   **深度**：实验展示了从需求到模型再到测试生成的全链路，逻辑严密。
    *   **广度**：虽然仅有一个案例，但该案例包含了复杂的嵌套数据结构（数组、结构体）和连续控制流，具有代表性。
    *   **局限性**：缺乏多行业、多规模案例的横向对比，尚未进行大规模压力测试或针对不同 MBT 算法的消融实验。

### 6. 主要结论与发现
*   **覆盖率显著提升**：在相同的初始场景集下，Pickles 通过自动组合场景，将转换覆盖率从传统 BDD 的 **30% 提升至 100%**。
*   **输入空间扩展**：通过参数化定义，Pickles 能够覆盖数以百计的有效输入组合（例如在案例中代表了 112 种可能的传感器配置），而传统 BDD 仅能覆盖约 **1.5%** 的手动硬编码输入。
*   **早期错误发现**：主模型的组合过程可以识别出不可达的场景或逻辑冲突，实现测试的“左移”。

### 7. 优点
*   **弥合鸿沟**：成功实现了“人类可读性”与“机器严谨性”的统一。
*   **双向透明**：测试用例以自然语言返回，方便非技术人员（如产品经理、审计员）理解测试结果。
*   **维护成本低**：只需修改 Pickles 规范，即可自动更新所有相关的形式化模型和测试用例。

### 8. 不足与局限
*   **迁移成本**：虽然 Pickles 接近自然语言，但用户仍需学习如何定义变量域和约束，这比纯 BDD 略显复杂。
*   **状态爆炸风险**：对于极其复杂的系统，模型组合可能会面临状态空间爆炸问题，论文未详细讨论其在大规模系统上的性能表现。
*   **人工依赖**：测试适配器（Test Adapters）的实现仍需人工编写代码，以连接抽象指令与具体被测系统。
*   **偏差风险**：实验仅基于单一工业案例，结果可能受特定领域逻辑的影响，普适性有待进一步验证。

（完）
