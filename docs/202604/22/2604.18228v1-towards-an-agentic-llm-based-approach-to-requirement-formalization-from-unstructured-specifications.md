---
title: Towards an Agentic LLM-based Approach to Requirement Formalization from Unstructured Specifications
title_zh: 迈向基于智能体大语言模型的非结构化规范需求形式化方法
authors: "Alberto Tagliaferro, Bruno Guindani, Livia Lestingi, Matteo Rossi"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18228v1"
tags: ["query:re-us"]
score: 9.0
evidence: 从非结构化规范中提取需求并进行形式化
tldr: "本研究针对安全关键系统中非结构化自然语言规范难以转化为形式化属性的问题，提出了一种基于智能体（Agentic）的LLM方法。该方法通过模块化流水线，包含需求提取、兼容性过滤及形式化翻译，确保了生成属性的语法正确性与语义一致性。实验表明，该方法在三个场景下达到了77.8%的准确率，有效弥补了非正式描述与形式化验证之间的鸿沟。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18228v1/fig-001.webp\", \"caption\": \"Table 2: Distribution of requirement extraction accuracy across the evaluated scenarios.\", \"page\": 8, \"index\": 1, \"width\": 853, \"height\": 562}]"
motivation: 传统的LLM方法在将自然语言规范转化为形式化属性时，往往难以保证语义的一致性，且难以满足验证工具的约束。
method: 提出一种模块化的智能体流水线，集成了需求提取、针对目标形式化语言的兼容性过滤以及最终的形式化翻译。
result: "在三个不同场景的实验中，该方法生成的属性在语法和语义上均表现良好，准确率达到77.8%。"
conclusion: 该研究证明了利用AI智能体自动化提取并转化可验证形式化属性的可行性，为弥合非正式规范与形式化验证之间的差距奠定了基础。
---

## 摘要
安全关键系统的早期规范通常以自然语言表达，这使得推导适用于验证且保证安全所需的形式化属性变得困难。虽然近期基于大语言模型（LLM）的方法可以从文本生成形式化产物，但它们主要关注语法的正确性，而无法确保非正式需求与可形式化验证属性之间的语义一致性。我们提出了一种基于智能体的方法论，能够从非结构化规范中自动提取待验证属性。该模块化流水线结合了需求提取、针对目标形式系统的兼容性过滤以及向形式化属性的转换。在三个场景下的实验结果表明，该流水线生成的语法和语义一致的形式化属性准确率达到 77.8%。通过明确考虑建模和验证约束，该方法为利用人工智能（AI）弥合非正式描述与具有语义意义的形式化验证之间的鸿沟迈出了铺垫性的一步。

## Abstract
Early-stage specifications of safety-critical systems are typically expressed in natural language, making it difficult to derive formal properties suitable for verification and needed to guarantee safety. While recent Large Language Model (LLM)-based approaches can generate formal artifacts from text, they mainly focus on syntactic correctness and do not ensure semantic alignment between informal requirements and formally verifiable properties. We propose an agentic methodology that automatically extracts verification-ready properties from unstructured specifications. The modular pipeline combines requirement extraction, compatibility filtering with respect to a target formalism, and translation into formal properties. Experimental results across three scenarios show that the pipeline generates syntactically and semantically aligned formal properties with a 77.8% accuracy. By explicitly accounting for modeling and verification constraints, the approach is a paving step towards exploiting Artificial Intelligence (AI) to bridge the gap between informal descriptions and semantically meaningful formal verification.

---

## 论文详细总结（自动生成）

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在安全关键系统（如人机交互机器人）的开发早期，需求通常以非结构化的自然语言（NL）描述。为了进行形式化验证以确保安全性，必须将这些模糊的文本转化为精确的形式化属性（如时序逻辑）。
*   **痛点**：现有基于大语言模型（LLM）的方法虽能生成形式化代码，但往往只关注**语法正确性**（能否编译），而忽视了**语义一致性**（是否真实反映了原始需求）以及**可验证性**（是否符合目标模型的边界约束）。
*   **目标**：提出一种基于智能体（Agentic）的流水线，自动从非结构化规范中提取既符合语法又具备语义一致性的可验证属性。

### 2. 方法论：核心思想与关键技术细节
论文提出了一种三阶段的模块化智能体流水线，通过分工明确的 LLM 角色来降低任务复杂度：
1.  **阶段 1：需求提取（Requirement Extraction）**：
    *   利用 LLM 将非结构化文本转化为结构化的原子自然语言需求（JSON 格式）。
    *   采用 **Few-shot（少样本）提示策略**，引导模型识别实体、动态和约束。
2.  **阶段 2：可验证性分类（Verifiability Classification）**：
    *   充当“形式化守门员”。LLM 根据目标模型（如 LIrAs 生成的随机混合自动机）的语义边界，过滤掉无法验证的需求（如不可观测变量、主观设计意图）。
3.  **阶段 3：形式化翻译（Formal Translation）**：
    *   将过滤后的需求翻译为目标形式化语言（本研究中为 UPPAAL SMC 查询语句）。
    *   通过系统提示词强制执行 **BNF 范式语法约束**，确保生成的查询符合验证工具的输入标准。

### 3. 实验设计
*   **实验场景**：使用了三个典型的网络物理系统（CPS）场景：咖啡递送（SC1）、用户引导运输（SC2）和工厂流水线（SC3）。
*   **基准测试（Benchmark）**：研究者手动构建了地面真值（Ground Truth, GT），包含 71 条自然语言需求和 72 条对应的形式化查询。
*   **评估方法**：
    *   **LLM-as-a-Judge**：由于自然语言和形式化逻辑存在多种等价表达，研究者使用 LLM 作为裁判，结合逻辑等价规则（如交换律、空间对称性）评估语义一致性。
    *   **语法检查**：使用 UPPAAL 的 `verifyta` 编译器进行强制语法验证。

### 4. 资源与算力
*   **模型**：实验主要使用 `gemini-3.1-pro-preview` 模型。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方法是基于现成 LLM 的推理流水线（Prompt Engineering），而非模型微调，因此主要的资源消耗在于 API 调用和推理 Token。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 个复杂场景进行了端到端的测试，涵盖了从提取到翻译的全流程。
*   **充分性评估**：
    *   **客观性**：通过解耦三个阶段独立评估，避免了误差级联对单一阶段性能分析的干扰。
    *   **公平性**：使用了 Few-shot 交叉验证（用 SC1/SC2 的例子引导 SC3 的生成），保证了评估的严谨性。
    *   **局限性**：实验场景数量（3个）相对较少，且仅针对单一 LLM 模型进行了测试，缺乏不同模型（如 GPT-4, Claude 3.5）之间的横向对比。

### 6. 主要结论与发现
*   **高准确率**：最终生成的验证查询在语法上 95.8% 正确，在考虑语义等价后的综合准确率达到 **77.8%**。
*   **分类有效性**：可验证性分类阶段达到了 88.7% 的准确率，有效过滤了无效需求，减少了后续计算浪费。
*   **语义评估的重要性**：研究发现，简单的字符串匹配（Exact Match）会严重低估模型能力（仅 34.7%），而通过语义裁判识别逻辑等价性是评估形式化任务的关键。

### 7. 优点
*   **模块化设计**：将复杂任务拆解，提高了过程的可追溯性和可解释性。
*   **领域无关性**：虽然实验在机器人领域（HRI），但流水线逻辑可轻易迁移至其他形式化框架。
*   **语义对齐**：通过中间分类层解决了“模型能验证什么”与“用户想要什么”之间的脱节问题。

### 8. 不足与局限
*   **逻辑严谨性缺失**：LLM 在提取需求时偶尔会遗漏严格的逻辑边界（如防止死循环的条件）或根据常识“幻觉”出原文未提及的约束。
*   **抽象层级偏差**：模型有时会将高层操作过度分解为微观步骤，导致与预期的抽象层级不一致。
*   **规模限制**：目前仅在小型 CPS 场景验证，对于超大规模、多组件耦合的工业级规范，其处理能力尚待验证。

（完）
