---
title: Towards an Agentic LLM-based Approach to Requirement Formalization from Unstructured Specifications
title_zh: 迈向基于智能体大语言模型的非结构化规范需求形式化方法
authors: "Alberto Tagliaferro, Bruno Guindani, Livia Lestingi, Matteo Rossi"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18228v1"
tags: ["query:re-us"]
score: 8.0
evidence: 从非结构化规范中自动提取可验证属性
tldr: "针对安全关键系统早期规范难以形式化的问题，本文提出一种基于智能体（Agentic）的大模型方法，旨在从非结构化文本中自动提取可验证属性。该方法通过模块化流水线，结合需求提取、兼容性过滤及形式化转换，解决了现有方法在语义对齐上的不足。实验表明，该方法在保证语法正确的同时，实现了77.8%的语义对齐准确率，为弥合非正式描述与形式化验证之间的鸿沟提供了有效路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18228v1/fig-001.webp\", \"caption\": \"Table 2: Distribution of requirement extraction accuracy across the evaluated scenarios.\", \"page\": 8, \"index\": 1, \"width\": 853, \"height\": 562}]"
motivation: 传统的自然语言规范难以直接转化为用于安全验证的形式化属性，且现有大模型方法往往忽视了非正式需求与形式化表示之间的语义一致性。
method: 提出一种包含需求提取、形式化兼容性过滤和自动翻译的模块化智能体流水线，将非结构化规范转化为可验证的属性。
result: "在三种实验场景下，该方法生成的属性在语法和语义对齐方面达到了77.8%的准确率。"
conclusion: 该研究通过显式考虑建模与验证约束，成功利用人工智能技术缩小了非正式需求描述与语义明确的形式化验证之间的差距。
---

## 摘要
安全关键系统的早期规范通常以自然语言表达，这使得推导适用于验证且保证安全所需的形式化属性变得困难。虽然近期基于大语言模型（LLM）的方法可以从文本生成形式化产物，但它们主要关注语法的正确性，而无法确保非正式需求与可形式化验证属性之间的语义一致性。我们提出了一种基于智能体的方法论，能够从非结构化规范中自动提取待验证属性。该模块化流水线结合了需求提取、针对目标形式系统的兼容性过滤以及向形式化属性的转换。在三个场景下的实验结果表明，该流水线生成的语法和语义一致的形式化属性准确率达到 77.8%。通过明确考虑建模和验证约束，该方法为利用人工智能（AI）弥合非正式描述与具有语义意义的形式化验证之间的鸿沟迈出了铺垫性的一步。

## Abstract
Early-stage specifications of safety-critical systems are typically expressed in natural language, making it difficult to derive formal properties suitable for verification and needed to guarantee safety. While recent Large Language Model (LLM)-based approaches can generate formal artifacts from text, they mainly focus on syntactic correctness and do not ensure semantic alignment between informal requirements and formally verifiable properties. We propose an agentic methodology that automatically extracts verification-ready properties from unstructured specifications. The modular pipeline combines requirement extraction, compatibility filtering with respect to a target formalism, and translation into formal properties. Experimental results across three scenarios show that the pipeline generates syntactically and semantically aligned formal properties with a 77.8% accuracy. By explicitly accounting for modeling and verification constraints, the approach is a paving step towards exploiting Artificial Intelligence (AI) to bridge the gap between informal descriptions and semantically meaningful formal verification.

---

## 论文详细总结（自动生成）

以下是对论文《Towards an Agentic LLM-based Approach to Requirement Formalization from Unstructured Specifications》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在安全关键系统（如人机交互机器人）的早期开发阶段，需求通常以非结构化的自然语言（NL）描述。为了进行形式化验证以确保安全性，必须将这些描述转化为精确的形式化属性。
*   **核心问题**：现有的基于大语言模型（LLM）的方法虽然能生成形式化代码，但往往只关注**语法正确性**（能否编译），而忽视了**语义对齐**（是否真实反映了原始意图）以及**可验证性**（生成的属性是否超出了目标模型的表达能力）。
*   **研究目标**：提出一种基于智能体（Agentic）的流水线，旨在从非结构化规范中自动提取既符合语法又具备语义一致性、且在目标模型中可验证的形式化属性。

### 2. 方法论
该论文提出了一种包含三个阶段的模块化智能体流水线：
1.  **需求提取（Requirement Extraction）**：
    *   使用 LLM 将非结构化文本转化为结构化的原子自然语言需求（JSON 格式）。
    *   采用 **Few-shot（少样本）提示策略**，引导模型识别实体、动态和约束，并规范化表达（如“系统必须……”）。
2.  **可验证性分类（Verifiability Classification）**：
    *   充当“形式化守门员”。LLM 扮演专家角色，根据目标模型（如随机混合自动机 SHA）的语义边界和假设，过滤掉无法验证的需求（如不可观察的变量、主观设计意图）。
3.  **形式化翻译（Formal Translation）**：
    *   将筛选后的需求翻译为目标形式化语言（本研究中为 **UPPAAL SMC** 查询语句）。
    *   **关键技术**：在提示词中强制执行 **BNF 范式语法约束**，并提供映射规则，确保生成的查询符合验证工具的输入要求。
    *   **语义评估**：引入“LLM-as-a-judge”机制，识别逻辑等价但语法不同的表达式（如 $A \land B$ 与 $B \land A$）。

### 3. 实验设计
*   **实验场景**：使用了三个典型的网络实体系统（CPS）场景：
    1.  **SC1: Coffee_Delivery**（咖啡配送机器人）
    2.  **SC2: User_Guided_Transport**（用户引导运输）
    3.  **SC3: Factory_Pipeline**（工厂流水线）
*   **基准测试（Benchmark）**：研究者手动构建了包含 71 条自然语言需求、52 条可验证需求和 72 条形式化查询的**地面真值（Ground Truth, GT）**数据集。
*   **对比与评估指标**：
    *   针对提取阶段：评估语义匹配度（完全匹配、部分匹配、无匹配）。
    *   针对分类阶段：评估准确率、精确率和召回率。
    *   针对翻译阶段：评估语法正确率（通过 UPPAAL 编译器）和语义准确率（结合 LLM 判定逻辑等价性）。

### 4. 资源与算力
*   **模型使用**：实验主要基于 **`gemini-3.1-pro-preview`** 模型。
*   **算力细节**：文中**未明确说明**具体的硬件算力（如 GPU 型号或训练时长），因为该研究侧重于利用现有的商业 LLM API 进行推理和智能体工作流构建，而非从头训练模型。实验中将模型温度（Temperature）设为 0.1 以降低随机性。

### 5. 实验数量与充分性
*   **实验规模**：共针对 3 个场景进行了测试，涉及 71 条原始需求。
*   **充分性评价**：
    *   **优点**：实验对流水线的每一个阶段都进行了独立评估，避免了误差级联对单一指标的影响，评估维度较为全面（涵盖语法、语义、逻辑等价性）。
    *   **局限**：样本量（3 个场景）相对较小，且主要集中在人机交互（HRI）领域，跨领域的泛化能力尚需进一步验证。

### 6. 主要结论与发现
*   **提取能力**：LLM 能有效提取核心功能逻辑，但在处理严格的逻辑边界（如防止死循环的条件）时存在疏漏，且容易产生基于领域知识的“幻觉”约束。
*   **分类有效性**：可验证性分类阶段达到了 **88.7% 的准确率**和 **94.2% 的召回率**，证明 LLM 能有效识别模型边界。
*   **翻译质量**：生成的查询语句**语法正确率高达 95.8%**。
*   **语义对齐**：如果仅采用严格的字符串匹配，准确率仅为 34.7%；但在引入 LLM 判定逻辑等价性后，**有效语义准确率提升至 77.8%**。这表明 LLM 在理解形式化逻辑的多种表达方式上具有显著优势。

### 7. 优点
*   **模块化设计**：将复杂任务分解为提取、过滤、翻译三个阶段，提高了过程的可追溯性和可解释性。
*   **显式边界意识**：通过在提示词中加入模型假设，解决了 LLM 容易生成“正确但不可验证”属性的问题。
*   **语义对齐评估**：超越了简单的语法检查，提出了基于逻辑等价性的语义评估方法，更符合形式化验证的实际需求。

### 8. 不足与局限
*   **领域局限性**：目前仅在 HRI 和 CPS 领域验证，对于更复杂的分布式系统或纯软件系统的适用性未知。
*   **幻觉风险**：LLM 仍会引入原文中未提及的约束，这在安全关键系统中可能导致验证结果的偏差。
*   **依赖 Judge 模型**：语义评估依赖于另一个 LLM 的判断，虽然提高了灵活性，但也引入了潜在的评估偏差。
*   **规模限制**：实验场景数量有限，缺乏大规模工业级规范的压力测试。

（完）
