---
title: "From Helpful to Trustworthy: LLM Agents for Pair Programming"
title_zh: 从有用性到可信性：用于结对编程的大语言模型智能体
authors: Ragib Shahariar Ayon
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10300v1"
tags: ["query:re-us"]
score: 8.0
evidence: 将非正式问题陈述转化为需求和正式规范
tldr: 本研究针对LLM编程助手输出虽合理但可能偏离开发者意图且难以审计的问题，提出了一种多智能体结对编程框架。通过将非正式需求转化为形式化规范、利用自动化工具进行迭代验证，以及支持代码重构与迁移，旨在提升LLM在真实开发场景中的可信度、可维护性和透明度，为构建可靠的编程助手提供理论与实践指导。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的LLM编程助手生成的代码往往缺乏对开发者意图的准确对齐和足够的审计证据，导致其在复杂演进项目中的可信度受限。
method: 提出一种多智能体协作流程，通过需求形式化、基于自动化反馈的迭代验证以及支持行为保持的维护任务来构建系统。
result: 预计将揭示多智能体工作流在何种情况下能显著增强信任，并产出一套构建可靠编程助手的实践指南。
conclusion: 该研究将通过外部化意图和工具化验证，推动LLM从单纯的辅助工具向可信的结对编程伙伴转型。
---

## 摘要
基于大语言模型（LLM）的编程智能体正越来越多地被用于生成代码、测试和文档。然而，它们的输出虽然看似合理，却可能与开发者的意图不一致，且在不断演进的项目中为评审提供的证据有限。这限制了我们对如何构建 LLM 结对编程工作流的理解，以确保生成的产物在长期内保持可靠、可审计且可维护。为填补这一空白，本博士研究提出了一项关于多智能体 LLM 结对编程的系统性研究，旨在将意图显式化，并利用开发工具进行迭代验证。该计划包含三项研究：将非正式的问题陈述转化为符合标准的的需求和形式化规范；利用自动化反馈（如基于求解器的反例）优化测试和实现；以及在保持已验证行为的同时，支持包括重构、API 迁移和文档更新在内的维护任务。预期成果是更清晰地理解多智能体工作流在何时能增强信任，并为构建适用于真实世界开发的可靠编程助手提供实践指导。

## Abstract
LLM-based coding agents are increasingly used to generate code, tests, and documentation. Still, their outputs can be plausible yet misaligned with developer intent and provide limited evidence for review in evolving projects. This limits our understanding of how to structure LLM pair-programming workflows so that artifacts remain reliable, auditable, and maintainable over time. To address this gap, this doctoral research proposes a systematic study of multi-agent LLM pair programming that externalizes intent and uses development tools for iterative validation. The plan includes three studies: translating informal problem statements into standards aligned requirements and formal specifications; refining tests and implementations using automated feedback, such as solver-backed counterexamples; and supporting maintenance tasks, including refactoring, API migrations, and documentation updates, while preserving validated behavior. The expected outcome is a clearer understanding of when multi-agent workflows increase trust, along with practical guidance for building reliable programming assistants for real-world development.

---

## 论文详细总结（自动生成）

以下是对论文《From Helpful to Trustworthy: LLM Agents for Pair Programming》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管基于大语言模型（LLM）的编程助手在生成代码和测试方面表现出色，但其输出往往存在“看似合理但偏离意图”的问题。开发者难以完全信任这些工具，因为它们缺乏透明的审计证据，且在项目长期演进（如重构、迁移）中难以保证行为的一致性。
*   **研究动机**：现有的 LLM 辅助开发多关注于“有用性”（Helpful），而本研究旨在将其转向“可信性”（Trustworthy）。通过构建多智能体协作流，将开发者的非正式意图转化为可验证的形式化规范，从而为代码的正确性提供确定性的证据。

### 2. 论文提出的方法论
*   **核心思想**：采用**驱动者-导航者（Driver-Navigator）**的多智能体系统（MAS）架构。
    *   **Driver 智能体**：负责提出代码实现、测试用例或文档。
    *   **Navigator 智能体**：负责评审 Driver 的产物。
*   **关键技术细节**：
    *   **约束化评审**：为了避免 Navigator（也是 LLM）产生不可靠的自由文本评价，研究将其输出限制为**机器可检查的契约（Contracts）和形式化规范**。
    *   **外部验证闭环**：引入确定性验证器（如 SMT 求解器）。验证器返回的证明或反例（Counterexamples）作为反馈，引导智能体进行迭代优化。
    *   **信任转移**：将对模型“直觉”的信任转移到“外部可验证证据”上，开发者只需确认形式化规范是否符合其原始意图。
*   **工作流阶段**：
    1.  **意图外部化**：将非正式需求转化为标准需求文档和形式化规范（如 JML）。
    2.  **迭代验证**：利用自动化反馈（反例）精炼代码和测试。
    3.  **行为保持维护**：在重构或 API 迁移时，利用已有的规范和测试作为约束，防止功能退化。

### 3. 实验设计
*   **前期实验（已完成）**：
    *   **AutoReSpec 框架**：针对 JML 规范合成，使用了包含 **72 个程序** 的基准测试集。
    *   **AutoJML 框架**：基于 ReAct 模式的智能体，使用了包含 **120 个程序** 的基准测试集，涵盖了多路径循环和嵌套循环等复杂控制流。
*   **对比方法**：与现有的 SOTA（先进水平）规范生成工具（如 SpecGen、AutoReSpec 的早期版本等）进行对比。
*   **未来计划实验**：计划在真实世界的结对编程场景中，对比“单智能体”与“多智能体（Driver-Navigator）”在测试通过率、失败重现性和维护任务中的表现。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练/推理时长。由于该研究主要基于现有的 LLM 进行 Prompt Engineering 和 Agent 编排，而非从头训练模型，其算力消耗主要集中在 API 调用和本地形式化验证器的运行上。

### 5. 实验数量与充分性
*   **实验规模**：前期研究分别使用了 72 和 120 个程序样本，这在形式化验证和规范生成的软件工程研究领域属于标准规模。
*   **充分性评价**：
    *   **初步充分**：针对特定任务（如 JML 规范生成）的实验较为扎实，包含了消融实验的思想（如对比不同复杂度的循环结构）。
    *   **待扩展**：作为博士研究计划，目前在“维护任务”和“大规模仓库演进”方面的实验尚处于计划阶段，需要更多真实世界的大型项目数据来验证其鲁棒性。

### 6. 论文的主要结论与发现
*   **验证器引导的有效性**：通过验证器反馈进行迭代，能显著提高 LLM 生成规范的正确性和完整性。AutoJML 在复杂控制流程序上的完整性达到了 79.3% 以上。
*   **协作优势**：多智能体协作（尤其是引入 Fallback 机制）比单一模型更能处理失败案例，减少了评估时间并提升了成功率。
*   **信任机制的建立**：通过将 LLM 的输出锚定在形式化规范和自动化工具上，可以为开发者提供可审计的证据，从而增强对 AI 辅助代码的信任。

### 7. 优点
*   **架构严谨**：通过 Driver-Navigator 角色分离，模拟了人类结对编程的最佳实践。
*   **逻辑闭环**：不依赖 LLM 的自我评估，而是引入硬性的形式化验证工具作为“真理来源”，解决了 LLM 幻觉问题。
*   **关注全生命周期**：研究不仅关注代码生成，还扩展到了软件维护和文档更新，具有较高的实用价值。

### 8. 不足与局限
*   **语言与工具链限制**：目前的研究主要集中在 Java 和 JML（Java 建模语言），对于缺乏成熟形式化验证工具链的语言（如 Python 的某些复杂库）可能难以直接应用。
*   **开发者负担**：虽然信任转移到了规范上，但开发者仍需具备阅读和确认形式化规范的能力，这可能增加非专家用户的认知负荷。
*   **规模化挑战**：在处理具有复杂依赖关系的仓库级代码时，形式化验证器的性能和 LLM 的上下文窗口限制可能成为瓶颈。

（完）
