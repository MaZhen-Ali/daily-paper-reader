---
title: "From Helpful to Trustworthy: LLM Agents for Pair Programming"
title_zh: 从“有用”到“可信”：用于结对编程的大语言模型智能体
authors: Ragib Shahariar Ayon
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10300v1"
tags: ["query:re-us"]
score: 8.0
evidence: 将非正式问题陈述转化为符合标准的软件需求
tldr: 本研究针对LLM编程助手输出虽具迷惑性但可能偏离开发者意图且难以审计的问题，提出了一种多智能体结对编程框架。该框架通过将非正式需求转化为形式化规范、利用自动化工具（如求解器）进行迭代验证，并支持代码维护任务，旨在提升LLM生成内容的可靠性与可维护性，为构建可信的编程助手提供理论与实践指导。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的LLM编程助手生成的代码往往缺乏意图对齐和审计证据，导致其在复杂演进项目中的可信度受限。
method: 提出一种多智能体协作流程，通过将非正式描述转化为形式化规范，并结合自动化反馈工具进行迭代验证与维护。
result: 预期将明确多智能体工作流在何种情况下能增强信任，并产出构建可靠编程助手的实践指南。
conclusion: 通过外部化意图和工具化验证，多智能体协作能有效提升LLM在软件开发全生命周期中的可信度和实用性。
---

## 摘要
基于大语言模型（LLM）的代码智能体正越来越多地被用于生成代码、测试和文档。然而，它们的输出虽然看似合理，却可能与开发者的意图不一致，且在不断演进的项目中为评审提供的证据有限。这限制了我们对如何构建 LLM 结对编程工作流的理解，以确保产出物在长期内保持可靠、可审计且可维护。为填补这一空白，本博士研究提出了一项关于多智能体 LLM 结对编程的系统性研究，通过将意图外部化并利用开发工具进行迭代验证。该计划包含三项研究：将非正式的问题陈述转化为符合标准的业务需求和形式化规范；利用自动化反馈（如基于求解器的反例）优化测试和实现；以及在保持已验证行为的同时，支持包括重构、API 迁移和文档更新在内的维护任务。预期成果是更清晰地理解多智能体工作流在何时能增加信任，并为构建适用于真实世界开发的可靠编程助手提供实践指导。

## Abstract
LLM-based coding agents are increasingly used to generate code, tests, and documentation. Still, their outputs can be plausible yet misaligned with developer intent and provide limited evidence for review in evolving projects. This limits our understanding of how to structure LLM pair-programming workflows so that artifacts remain reliable, auditable, and maintainable over time. To address this gap, this doctoral research proposes a systematic study of multi-agent LLM pair programming that externalizes intent and uses development tools for iterative validation. The plan includes three studies: translating informal problem statements into standards aligned requirements and formal specifications; refining tests and implementations using automated feedback, such as solver-backed counterexamples; and supporting maintenance tasks, including refactoring, API migrations, and documentation updates, while preserving validated behavior. The expected outcome is a clearer understanding of when multi-agent workflows increase trust, along with practical guidance for building reliable programming assistants for real-world development.

---

## 论文详细总结（自动生成）

这是一份关于论文《From Helpful to Trustworthy: LLM Agents for Pair Programming》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管基于大语言模型（LLM）的编程助手在生成代码和测试方面表现出色，但其输出往往具有“迷惑性”——看似正确实则偏离开发者意图，且缺乏可审计的证据。在长期演进的项目中，如何构建一个可靠、可审计且可维护的 LLM 协作工作流是当前的主要挑战。
*   **研究动机**：现有的 LLM 助手多为“有用”但非“可信”。作者希望通过多智能体协作（MAS）和形式化验证技术，将开发者的意图显式化，并利用确定性的验证工具（如 SMT 求解器）提供证据，从而将信任从对模型的盲目依赖转向对可验证证据的认可。

### 2. 方法论：核心思想与技术细节
该研究提出了一种基于**驾驶员-导航员（Driver-Navigator）**模式的多智能体系统，旨在实现端到端的证据驱动工作流：
*   **角色分工**：
    *   **驾驶员智能体（Driver Agent）**：负责提出代码实现、测试用例或文档等工件。
    *   **导航员智能体（Navigator Agent）**：负责审查工件。为了避免“用一个 LLM 审计另一个 LLM”的信任循环，导航员被约束为生成**机器可检查的契约（Contracts）和形式化规范**，而非自由文本评论。
*   **迭代验证流程**：
    1.  **意图外部化**：将非正式的自然语言需求转化为符合标准的业务需求和形式化规范（如 JML）。
    2.  **自动化反馈循环**：利用确定性验证器（Verifier）对生成的规范和代码进行检查。如果验证失败，验证器返回**反例（Counterexamples）**或证明失败信息。
    3.  **基于证据的修复**：智能体根据验证器提供的硬性反馈（而非模糊的建议）迭代优化代码和测试。
*   **维护支持**：在重构、API 迁移或文档更新时，利用已有的形式化规范作为行为约束，确保演进过程中的行为一致性。

### 3. 实验设计与 Benchmark
由于这是一篇博士研究计划（Doctoral Symposium）论文，它总结了已完成的初步实验并规划了未来实验：
*   **已完成实验（AutoReSpec & AutoJML）**：
    *   **数据集**：使用了包含 72 个程序（AutoReSpec）和 120 个程序（AutoJML）的基准测试集，涵盖了多路径循环、嵌套循环等复杂控制流模式。
    *   **对比方法**：对比了单智能体设置以及之前的形式化规范生成方法。
    *   **评估指标**：验证成功率（Success Probability）、规范完整性（Completeness）和评估耗时。
*   **未来实验规划**：
    *   **场景**：测试驱动开发（TDD）工作流、存储库级别的代码演进（重构、依赖升级）。
    *   **目标**：量化多智能体协作在提高可靠性方面相对于单智能体基线的提升。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **技术栈提及**：提到了使用 ReAct 模式的 LLM 智能体，并结合了 Web 检索增强（RAG）和 SMT 求解器后端。

### 5. 实验数量与充分性
*   **实验规模**：初步研究已在约 200 个程序样本上进行了验证，涵盖了从简单到复杂的算法逻辑。
*   **充分性评价**：
    *   **初步实验较为充分**：针对形式化规范生成（JML）进行了消融对比，证明了协作模式能显著提升复杂逻辑（如嵌套循环）的处理能力。
    *   **整体计划尚在进行中**：关于“维护任务”和“大规模存储库演进”的实验仍处于计划阶段，目前尚缺乏在真实大型工业项目中的验证数据。

### 6. 主要结论与发现
*   **协作优于单体**：多智能体协作（特别是引入 fallback 机制）能显著提高形式化规范生成的成功率（AutoReSpec 达到 58.2% 的成功概率）。
*   **验证器反馈至关重要**：利用验证器提供的错误信息进行迭代，能使 AutoJML 在复杂程序上的完整性达到 79.3%~85.7%。
*   **信任转移**：通过将 LLM 的输出转化为可验证的规范，开发者的职责简化为“确认规范是否符合意图”，从而降低了审计负担。

### 7. 优点（亮点）
*   **闭环验证**：不依赖 LLM 的自我感觉，而是引入了 SMT 求解器等硬性约束工具，增强了结果的科学性。
*   **角色解耦**：驾驶员与导航员的职责分离，模拟了人类结对编程的最佳实践，提高了代码审查的结构化程度。
*   **关注全生命周期**：研究不仅关注代码生成，还扩展到了软件维护和文档更新，具有较强的工程实用潜力。

### 8. 不足与局限
*   **规范编写门槛**：虽然 LLM 辅助生成规范，但开发者仍需具备理解形式化规范（如 JML）的能力，以确认其是否符合意图。
*   **验证器限制**：该方法高度依赖底层验证工具的能力，对于难以形式化描述的业务逻辑（如复杂的 UI 交互或非功能性需求），该框架的适用性受限。
*   **评估偏差风险**：目前的 Benchmark 多为算法类小程序，在处理具有复杂框架依赖（如 Spring Boot）的大型企业级应用时，可能会面临上下文窗口和环境模拟的挑战。

（完）
