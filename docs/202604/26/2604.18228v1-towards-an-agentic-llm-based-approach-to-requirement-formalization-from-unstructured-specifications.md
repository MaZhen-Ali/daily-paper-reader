---
title: Towards an Agentic LLM-based Approach to Requirement Formalization from Unstructured Specifications
title_zh: 迈向基于智能体大语言模型的非结构化规范需求形式化方法
authors: "Alberto Tagliaferro, Bruno Guindani, Livia Lestingi, Matteo Rossi"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18228v1"
tags: ["query:re-us"]
score: 9.0
evidence: 从非结构化文本中提取需求并形式化的代理方法
tldr: "针对安全关键系统早期规范多为非结构化自然语言、难以直接进行形式化验证的问题，本文提出一种基于智能体（Agentic）的LLM方法。该方法通过包含需求提取、兼容性过滤及形式化翻译的模块化流水线，自动将非结构化规范转化为语义对齐的形式化属性。实验表明，该方法在保证语法正确性的同时，显著提升了语义一致性，准确率达77.8%，为弥合非正式描述与形式化验证之间的鸿沟提供了新路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18228v1/fig-001.webp\", \"caption\": \"Table 2: Distribution of requirement extraction accuracy across the evaluated scenarios.\", \"page\": 8, \"index\": 1, \"width\": 853, \"height\": 562}]"
motivation: 现有的LLM生成形式化属性方法往往只关注语法正确性，而忽视了与原始非结构化需求之间的语义对齐，导致难以直接用于安全验证。
method: 提出一种模块化的智能体流水线，集成了需求提取、针对目标形式化语言的兼容性过滤以及最终的形式化属性翻译。
result: "在三种不同场景下的实验结果显示，该方法生成的属性在语法和语义上均表现良好，综合准确率达到77.8%。"
conclusion: 该研究通过显式考虑建模与验证约束，证明了利用AI自动化构建语义准确的形式化验证属性的可行性。
---

## 摘要
安全关键系统的早期规范通常以自然语言表达，这使得推导适用于验证且保证安全所需的规范属性变得困难。虽然近期基于大语言模型（LLM）的方法可以从文本生成形式化产物，但它们主要关注语法正确性，而无法确保非正式需求与可形式化验证属性之间的语义一致性。我们提出了一种基于智能体的方法论，能够从非结构化规范中自动提取可供验证的属性。该模块化流水线结合了需求提取、针对目标形式系统的兼容性过滤以及向形式化属性的转换。在三个场景下的实验结果表明，该流水线生成的语法和语义一致的形式化属性准确率达到 77.8%。通过明确考虑建模和验证约束，该方法为利用人工智能（AI）弥合非正式描述与具有语义意义的形式化验证之间的鸿沟奠定了基础。

## Abstract
Early-stage specifications of safety-critical systems are typically expressed in natural language, making it difficult to derive formal properties suitable for verification and needed to guarantee safety. While recent Large Language Model (LLM)-based approaches can generate formal artifacts from text, they mainly focus on syntactic correctness and do not ensure semantic alignment between informal requirements and formally verifiable properties. We propose an agentic methodology that automatically extracts verification-ready properties from unstructured specifications. The modular pipeline combines requirement extraction, compatibility filtering with respect to a target formalism, and translation into formal properties. Experimental results across three scenarios show that the pipeline generates syntactically and semantically aligned formal properties with a 77.8% accuracy. By explicitly accounting for modeling and verification constraints, the approach is a paving step towards exploiting Artificial Intelligence (AI) to bridge the gap between informal descriptions and semantically meaningful formal verification.

---

## 论文详细总结（自动生成）

以下是对论文《Towards an Agentic LLM-based Approach to Requirement Formalization from Unstructured Specifications》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在开发安全关键系统（如服务机器人）时，早期规范通常以非结构化的自然语言（NL）描述。为了确保系统安全，必须将这些非正式需求转化为可被形式化验证工具（如 UPPAAL）处理的属性。
*   **现有挑战**：目前的 LLM 方法虽然能生成形式化代码，但往往只关注**语法正确性**（能否运行），而忽视了**语义对齐**（生成的公式是否真实反映了原始意图）。
*   **研究动机**：开发一种能够自动从文本中提取、过滤并翻译为语义一致、可验证属性的智能体化（Agentic）方法，弥合自然语言与形式化方法之间的鸿沟。

### 2. 方法论：核心思想与技术细节
论文提出了一种模块化的智能体流水线，将复杂任务分解为三个专业化阶段：
1.  **需求提取（Stage 1: Requirements Extraction）**：
    *   利用 LLM 将非结构化文本转化为结构化的原子自然语言需求（JSON 格式）。
    *   采用 **Few-shot 提示策略**，引导模型识别实体、动态和约束，并标准化语法（如“系统必须...”）。
2.  **可验证性分类（Stage 2: Verifiability Classification）**：
    *   充当“形式化守门员”。LLM 扮演专家角色，根据目标模型（如随机混合自动机 SHA）的边界，判断需求是否可验证。
    *   过滤掉不可观测变量、定性设计意图或模型外部现象，减少后续阶段的无效计算。
3.  **形式化翻译（Stage 3: Formal Translation）**：
    *   将过滤后的需求翻译为目标形式化语言（本研究中为 UPPAAL SMC 查询语句）。
    *   通过系统提示词强制执行 BNF 范式约束，确保生成的查询符合语法规范。

### 3. 实验设计
*   **实验场景**：选取了三个典型的网络实体系统（CPS）场景：
    *   **SC1: Coffee_Delivery**（咖啡配送机器人）
    *   **SC2: User_Guided_Transport**（用户引导运输）
    *   **SC3: Factory_Pipeline**（工厂流水线）
*   **Benchmark（基准）**：研究者手动构建了地面真值（Ground Truth, GT），包含 71 个 NL 需求和 72 个对应的形式化查询。
*   **对比与评估方法**：
    *   **语法检查**：使用 UPPAAL 的 `verifyta` 编译器。
    *   **语义评估**：采用 **LLM-as-a-judge** 机制。由于形式化表达具有多样性（如逻辑等价性），传统的字符串匹配（Exact Match）会低估性能，因此引入 LLM 判断生成的查询与 GT 是否在逻辑上等价。

### 4. 资源与算力
*   **模型型号**：实验统一使用了 `gemini-3.1-pro-preview`。
*   **参数设置**：温度（Temperature）设为 0.1 以降低随机性，输出格式限制为 `application/json`。
*   **算力说明**：文中**未明确提及**具体的硬件资源（如 GPU 型号、数量）或推理总时长。由于该研究侧重于 Prompt Engineering 和 Agentic Workflow 的应用而非模型训练，其算力消耗主要体现在 API 调用上。

### 5. 实验数量与充分性
*   **实验规模**：涵盖 3 个场景，共 71 条需求。
*   **充分性评价**：
    *   **客观性**：通过解耦三个阶段独立测试，避免了误差级联对单一阶段评估的影响。
    *   **公平性**：使用了交叉验证式的 Few-shot 学习（用另外两个场景的 GT 作为当前场景的示例）。
    *   **局限性**：实验场景数量较少（仅 3 个），且均集中在人机交互（HRI）领域，对于更广泛工业场景的泛化能力尚待验证。

### 6. 主要结论与发现
*   **提取能力**：LLM 在需求提取阶段表现良好，准确率（完全+部分匹配）达 **81.8%**，但容易引入“常识性幻觉”（添加原文没有的约束）。
*   **分类精度**：可验证性分类的准确率达 **88.7%**，召回率高达 **94.2%**，有效过滤了无法形式化的需求。
*   **翻译质量**：生成的查询语法正确率极高（**95.8%**）。
*   **语义对齐**：在考虑逻辑等价性的情况下，最终语义准确率为 **77.8%**。实验证明，简单的字符串匹配会严重低估 LLM 的表现（从 34.7% 提升至 77.8%）。

### 7. 优点与亮点
*   **模块化设计**：将提取、过滤、翻译分离，提高了过程的可追溯性和可解释性。
*   **语义对齐关注**：不同于以往只看语法的研究，本文重点解决了“生成的公式是否正确表达了需求”这一核心痛点。
*   **形式化约束集成**：在提示词中显式引入建模假设和 BNF 语法，显著提升了生成产物的可用性。

### 8. 不足与局限
*   **逻辑严密性不足**：LLM 有时会遗漏严格的逻辑边界（如防止死循环的条件）或任务完成的保证。
*   **幻觉风险**：模型倾向于根据领域知识补充规范中未提及的约束。
*   **评估依赖性**：语义评估高度依赖于作为“裁判”的 LLM，虽然引入了形式化等价规则，但仍存在潜在的判定偏差。
*   **规模限制**：目前的评估数据集较小，且目标语言仅限于 UPPAAL SMC，尚未测试其他复杂的形式化逻辑（如 LTL/CTL）。

（完）
