---
title: "PICKLES: a Natural Language Framework for Requirement Specification and Model-Based Testing"
title_zh: PICKLES：一种用于需求规范和基于模型测试的自然语言框架
authors: "María Belén Rodríguez, Petra van den Bos"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26572v1"
tags: ["query:re-us"]
score: 8.0
evidence: 用于需求规范和行为驱动开发场景的自然语言框架
tldr: PICKLES是一个结合了行为驱动开发（BDD）的可读性与基于模型测试（MBT）的自动化能力的框架。它通过扩展Gherkin语言，实现了从结构化自然语言场景到形式化模型的双向转换，并能自动生成高覆盖率的测试用例。该方法在工业级交通管理系统中得到了验证，显著提升了测试覆盖率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26572v1/fig-001.webp\", \"caption\": \"Figure 3 Smaster. Each line style (dashed, dotted, solid, dash-dotted) denotes transitions from S1–S4. Variables follow the reference in Table 1.\", \"page\": 10, \"index\": 1, \"width\": 1021, \"height\": 549}, {\"url\": \"assets/figures/arxiv/2604.26572v1/fig-002.webp\", \"caption\": \"Figure 2 STS1: Faulty detectors outside the critical section.\", \"page\": 8, \"index\": 2, \"width\": 520, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.26572v1/fig-003.webp\", \"caption\": \"Figure 4 Coverage of STSmaster. Transitions and states covered by only executing the specification scenarios are shown in black, whereas transitions in red show non-tested transitions.\", \"page\": 12, \"index\": 3, \"width\": 509, \"height\": 367}]"
motivation: 旨在解决BDD在测试生成自动化程度上的不足，同时保留其需求规格说明的可读性。
method: 提出PICKLES语言并实现其与形式化模型的双向转换，通过构建主模型并应用MBT算法自动导出测试用例。
result: 在工业案例研究中，PICKLES在相同场景集下实现了比传统BDD显著更高的测试覆盖率。
conclusion: PICKLES成功融合了BDD与MBT的优势，为需求规格说明和自动化模型测试提供了一种高效且易读的解决方案。
---

## 摘要
本文结合了基于模型测试（MBT）和行为驱动开发（BDD）领域的方法，定义了一种测试方法。该方法既具有 BDD 中人类可读的规范和测试用例，又利用了 MBT 的建模技术和自动测试生成算法。我们引入了 PICKLES（一种用于测试场景的精确输入和控制流关键字语言），它是对 Gherkin 风格 BDD 场景的扩展，采用结构化自然语言进行规范。我们提供了从 Pickles 场景到形式化模型的双向转换，其中规范和测试均保持人类可读，并提供了一种获取所谓“主模型”（master model）的方法，该模型结合了所有转换后的场景。随后可以应用标准的 MBT 算法从中自动导出测试用例。我们实现了转换和组合步骤的原型，并将其应用于一个工业案例研究：交通管理系统中的一个软件组件。通过该案例，我们展示了整个流程，并证明了在相同场景集下，Pickles 相比于 BDD 能够实现显著更高的覆盖率。

## Abstract
This paper combines methods from the fields of Model-Based Testing (MBT) and Behaviour-Driven Development (BDD) to define a testing approach with human-readable specifications and test cases, as in BDD, while using the modelling techniques and automatic test generation algorithms from MBT. We introduce PICKLES, a Precise Input and Control-flow Keyword-based Language for tEst Scenarios; an extension of Gherkin-style BDD scenarios, specified in structured natural language. We provide a bi-directional translation from Pickles scenarios to formal models, where both specifications and tests are human-readable, and a method to obtain a so-called master model combining all translated scenarios. Standard MBT algorithms can then be applied to automatically derive test cases from it. We implement a prototype of the translation and composition steps, which we use on an industrial case study: a software component from a traffic management system. With it, we illustrate the pipeline and show how Pickles can achieve significantly higher coverage with respect to BDD from the same set of scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《PICKLES: a Natural Language Framework for Requirement Specification and Model-Based Testing》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在关键系统的开发中，测试至关重要。目前工业界存在两种主流但对立的测试范式：
*   **基于模型的测试 (MBT)**：通过形式化模型自动生成测试用例，具有极高的精确度和系统性覆盖能力，但由于模型定义复杂、不可读，非技术利益相关者难以参与。
*   **行为驱动开发 (BDD)**：使用 Gherkin 等自然语言（Given-When-Then）编写场景，可读性强，但本质上是基于“示例”的，难以处理复杂的控制流和大规模输入空间，且维护成本高。

**核心问题**：如何结合 BDD 的可读性与 MBT 的严谨性与自动化能力？论文提出了 **PICKLES** 框架，旨在通过一种结构化的自然语言实现需求规范与自动化模型测试的统一。

### 2. 方法论：核心思想与关键技术细节
PICKLES 的核心是一个**双向转换框架**，其主要流程如下：

*   **PICKLES DSL**：对 Gherkin 语法进行了扩展。除了传统的步骤描述，它引入了 `Variable Settings` 块，允许显式定义变量类型（原始类型、数组、结构体）、取值范围和约束条件。
*   **形式化映射（Pickles $\leftrightarrow$ STS）**：
    *   **语义映射**：将 Pickles 场景映射为**符号迁移系统 (Symbolic Transition Systems, STS)**。`Given` 映射为前置条件/初始守卫，`When` 映射为输入迁移，`Then` 映射为输出迁移。
    *   **变量处理**：将 DSL 中的变量和约束转换为 STS 中的位置变量、参数、守卫（Guards）和赋值（Assignments）。
*   **模型组合（Master Model Construction）**：
    *   **选择组合 ($\biguplus$)**：合并多个场景的初始位置，允许系统从初始状态选择执行任一场景。
    *   **顺序组合 ($\rhd$)**：将一个场景的终点与所有场景的起点连接，模拟系统在运行过程中的连续交互。
    *   **主模型**：通过组合所有场景 STS，生成一个能够反映系统整体运行逻辑的统一模型。
*   **测试生成与回译**：利用标准的 MBT 算法从主模型中导出覆盖所有迁移的路径，并使用 SMT 求解器确定具体的输入值。最后，将这些形式化路径重新翻译成人类可读的 Pickles 测试脚本。

### 3. 实验设计
*   **实验场景**：使用了荷兰 Technolution B.V. 公司的一个**工业级交通管理系统组件**作为案例。该组件负责接收传感器遥测数据，检测道路上的异常事件（如低速或静止车辆），并根据地理位置（是否在关键路段）向 UI 发送状态。
*   **Benchmark（基准）**：对比了传统的 **BDD 方法**（即手动编写的、基于固定示例的 Cucumber 测试用例）。
*   **对比维度**：
    1.  **迁移覆盖率 (Transition Coverage)**：衡量测试用例覆盖系统逻辑路径的比例。
    2.  **输入覆盖率 (Input Coverage)**：衡量在定义的变量域内，测试所涵盖的输入组合比例。

### 4. 资源与算力
*   **硬件/算力**：论文**未明确说明**具体的硬件配置（如 GPU/CPU 型号）。由于该研究属于形式化方法和软件工程领域，主要依赖于逻辑解析和 SMT 求解（如使用 Python 的 Lark 库进行语法解析），而非大规模深度学习训练，因此对算力的需求相对较低，普通工作站即可胜任。
*   **软件实现**：使用 Python 开发了原型工具，集成了 Lark 解析器，并支持将 STS 导出为 JSON 格式。

### 5. 实验数量与充分性
*   **实验规模**：研究基于 4 个核心工业场景进行了深入分析。
*   **充分性评价**：
    *   **逻辑验证充分**：通过一个真实的工业案例完整展示了从需求定义到模型合成，再到测试生成的全链路。
    *   **对比客观**：通过数学计算（如组合数学计算输入空间）量化了 PICKLES 相比 BDD 的优势。
    *   **局限性**：实验仅限于单一的工业案例研究，虽然具有代表性，但尚未在多种不同类型的系统（如高并发分布式系统或非确定性系统）上进行大规模基准测试。

### 6. 论文的主要结论与发现
*   **覆盖率显著提升**：在相同的初始场景集下，PICKLES 自动生成的测试用例实现了 **100% 的迁移覆盖率**，而传统 BDD 仅能达到约 **30%**（提升了 70 个百分点）。
*   **输入空间扩展**：通过参数化定义，PICKLES 能够覆盖数以百计的有效输入组合（如案例中计算出 112 种输入），而 BDD 通常只覆盖 1.5% 的可能输入。
*   **可读性保持**：生成的测试用例依然保持了 Given-When-Then 的自然语言格式，非技术人员可以直接理解测试意图。
*   **维护成本降低**：只需维护少量的通用场景，即可自动生成大量针对边界值和逻辑组合的测试。

### 7. 优点
*   **桥接鸿沟**：成功解决了 MBT 的“建模难”和 BDD 的“覆盖低”之间的矛盾。
*   **双向透明**：不仅规范是可读的，生成的测试结果也是可读的，形成了良好的反馈环。
*   **形式化严谨**：基于 STS 和 SMT 求解，确保了测试生成的逻辑正确性和边界覆盖。
*   **工业实用性**：直接兼容现有的 BDD 工具链，降低了企业采纳 MBT 技术的门槛。

### 8. 不足与局限
*   **路径可达性问题**：目前的模型组合算法可能会产生逻辑上不可达的路径（因为原型工具尚未在组合阶段完全集成路径条件的实时过滤）。
*   **表达能力限制**：目前的 DSL 尚不支持非确定性行为、并行处理以及复杂的实时时间约束（Timing constraints）。
*   **人工干预**：虽然测试生成是自动的，但将 Pickles 步骤连接到实际代码的“测试适配器（Test Adapters）”仍需人工编写。
*   **用户研究缺失**：论文缺乏关于非技术利益相关者在使用该 DSL 时的易用性评估（Empirical user study）。

（完）
