---
title: "PICKLES: a Natural Language Framework for Requirement Specification and Model-Based Testing"
title_zh: PICKLES：一种用于需求规范和基于模型测试的自然语言框架
authors: "María Belén Rodríguez, Petra van den Bos"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26572v1"
tags: ["query:re-us"]
score: 9.0
evidence: 用于需求规范和测试的Gherkin风格BDD场景
tldr: 本研究结合了基于模型测试（MBT）和行为驱动开发（BDD）的优势，提出了PICKLES框架。该框架引入了一种扩展自Gherkin的结构化自然语言，用于编写可读性强的需求规范。通过将PICKLES场景双向转换为形式化模型并构建统一的主模型，实现了测试用例的自动生成。在工业级交通管理系统案例中，该方法在保持高可读性的同时，显著提升了测试覆盖率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26572v1/fig-001.webp\", \"caption\": \"Figure 3 Smaster. Each line style (dashed, dotted, solid, dash-dotted) denotes transitions from S1–S4. Variables follow the reference in Table 1.\", \"page\": 10, \"index\": 1, \"width\": 1021, \"height\": 549}, {\"url\": \"assets/figures/arxiv/2604.26572v1/fig-002.webp\", \"caption\": \"Figure 2 STS1: Faulty detectors outside the critical section.\", \"page\": 8, \"index\": 2, \"width\": 520, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.26572v1/fig-003.webp\", \"caption\": \"Figure 4 Coverage of STSmaster. Transitions and states covered by only executing the specification scenarios are shown in black, whereas transitions in red show non-tested transitions.\", \"page\": 12, \"index\": 3, \"width\": 509, \"height\": 367}]"
motivation: 旨在结合BDD的人类可读性与MBT的形式化建模及自动测试生成能力，解决传统测试方法在直观性与严谨性之间的矛盾。
method: 提出了PICKLES语言及其与形式化模型的双向转换机制，通过整合多个场景构建主模型，并应用标准MBT算法自动派生测试用例。
result: 原型系统在工业级交通管理组件的案例研究中，证明了在相同场景集下，PICKLES比传统BDD方法能实现显著更高的测试覆盖率。
conclusion: PICKLES框架成功弥合了自然语言需求规范与形式化模型测试之间的鸿沟，为复杂软件系统的验证提供了一种高效且易于理解的方案。
---

## 摘要
本文结合了基于模型测试（MBT）和行为驱动开发（BDD）领域的方法，定义了一种测试方法。该方法既具有 BDD 中人类可读的规范和测试用例，又利用了 MBT 的建模技术和自动测试生成算法。我们引入了 PICKLES（一种用于测试场景的精确输入和控制流关键字语言），它是对 Gherkin 风格 BDD 场景的扩展，采用结构化自然语言进行规范。我们提供了从 Pickles 场景到形式化模型的双向转换，其中规范和测试均为人类可读，并提供了一种获取所谓“主模型”（master model）的方法，该模型结合了所有转换后的场景。随后可以应用标准的 MBT 算法从中自动导出测试用例。我们实现了转换和组合步骤的原型，并将其应用于一个工业案例研究：交通管理系统中的一个软件组件。通过该案例，我们展示了整个流程，并证明了在相同场景集下，Pickles 相比 BDD 能实现显著更高的覆盖率。

## Abstract
This paper combines methods from the fields of Model-Based Testing (MBT) and Behaviour-Driven Development (BDD) to define a testing approach with human-readable specifications and test cases, as in BDD, while using the modelling techniques and automatic test generation algorithms from MBT. We introduce PICKLES, a Precise Input and Control-flow Keyword-based Language for tEst Scenarios; an extension of Gherkin-style BDD scenarios, specified in structured natural language. We provide a bi-directional translation from Pickles scenarios to formal models, where both specifications and tests are human-readable, and a method to obtain a so-called master model combining all translated scenarios. Standard MBT algorithms can then be applied to automatically derive test cases from it. We implement a prototype of the translation and composition steps, which we use on an industrial case study: a software component from a traffic management system. With it, we illustrate the pipeline and show how Pickles can achieve significantly higher coverage with respect to BDD from the same set of scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《PICKLES: a Natural Language Framework for Requirement Specification and Model-Based Testing》** 的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在关键系统的开发中，**基于模型测试 (MBT)** 能够通过形式化模型自动生成高覆盖率的测试用例，但其门槛极高，要求人员具备深厚的形式化规范背景，导致工业界采纳率低。相比之下，**行为驱动开发 (BDD)** 使用 Gherkin 等自然语言（Given-When-Then 格式）编写场景，易于沟通但缺乏严谨性，且难以处理复杂的控制流和大规模测试覆盖。

论文提出了 **PICKLES** 框架，旨在弥合这两者之间的鸿沟。其核心目标是：**让非技术利益相关者能用人类可读的自然语言编写规范，同时利用 MBT 的严谨性和自动化能力生成高覆盖率的测试用例。**

### 2. 论文提出的方法论
PICKLES 框架通过以下核心技术实现 BDD 与 MBT 的融合：

*   **PicklesDSL**：对标准 Gherkin 语法的扩展。它保留了 Given-When-Then 结构，但引入了“变量设置块”（Variable Settings Block），允许显式定义变量类型（原始类型、数组、结构体）、取值范围和约束条件。
*   **双向转换机制**：
    *   **正向**：将 Pickles 场景自动转换为 **符号迁移系统 (Symbolic Transition Systems, STS)**。Given 映射为前置条件/初始迁移，When 映射为输入迁移，Then 映射为输出迁移。
    *   **逆向**：将 MBT 算法生成的形式化测试用例（包含具体的参数值）转换回 Pickles 格式的自然语言脚本，以便于人类阅读和 BDD 工具执行。
*   **主模型合成 (Master Model Composition)**：
    *   定义了**选择组合 ($\biguplus$)** 和**顺序组合 ($\triangleright$)** 操作符。
    *   将多个独立的 Pickles 场景（部分模型）融合成一个统一的“主模型”。这使得测试生成算法能够探索场景之间的交互，而不仅仅是孤立地测试单个场景。
*   **自动化流程**：从自然语言规范 $\rightarrow$ STS 模型 $\rightarrow$ 主模型 $\rightarrow$ 自动生成测试路径 $\rightarrow$ 求解器（SMT Solver）寻找具体输入值 $\rightarrow$ 自然语言测试脚本。

### 3. 实验设计
*   **场景/数据集**：使用了一个真实的工业案例研究——荷兰 Technolution B.V. 公司的**交通管理系统组件**。该组件负责处理道路传感器数据，根据故障检测器的位置（是否在关键路段）和数量来报告系统可用性。
*   **Benchmark（基准）**：以该公司现有的、基于 Cucumber 实现的传统 BDD 测试套件为基准。
*   **对比方法**：对比了“传统 BDD 方法（手动编写固定值的场景）”与“PICKLES 框架（基于相同场景自动生成测试）”在**迁移覆盖率 (Transition Coverage)** 和 **输入空间覆盖率 (Input Coverage)** 上的表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**使用了高性能计算资源（如 GPU）。
*   **实现细节**：该框架使用 Python 开发了一个原型工具，利用 **Lark 库** 进行 EBNF 语法解析。由于 STS 模型的路径求解涉及符号执行，通常会依赖 SMT 求解器（如 Z3），但文中未详细列出具体的硬件配置或训练时长（因为该方法属于逻辑推理而非深度学习训练）。

### 5. 实验数量与充分性
*   **实验规模**：论文基于 4 个核心业务场景构建了规范套件。
*   **充分性评价**：
    *   **深度充分**：通过一个复杂的工业级案例完整展示了从需求到测试生成的全链路，具有较强的说服力。
    *   **广度局限**：目前仅在一个工业案例上进行了验证，缺乏跨行业、跨规模的多样本对比。
    *   **客观性**：实验通过量化的覆盖率数据（70% 的迁移覆盖率提升）直观展示了方法优势，对比过程相对公平，因为它是在相同的初始场景输入下进行的比较。

### 6. 论文的主要结论与发现
*   **覆盖率显著提升**：在相同的 4 个初始场景下，传统 BDD 仅能覆盖主模型中约 30% 的迁移，而 PICKLES 通过自动组合场景实现了 **100% 的迁移覆盖率**。
*   **输入空间扩展**：通过参数化定义，PICKLES 能够从单个场景中派生出数百个有效的输入组合（例如在案例中将输入覆盖率提升了 98.5%），有效识别了边界值风险。
*   **降低维护成本**：由于测试用例是自动生成的，当需求变更时，只需修改自然语言规范，而无需手动更新成百上千个测试脚本。

### 7. 优点
*   **易用性与严谨性的平衡**：成功让非技术人员参与到形式化建模的过程中，无需学习复杂的数学符号。
*   **双向透明**：不仅规范是可读的，生成的测试用例也是可读的，这对于受监管行业的合规性报告至关重要。
*   **场景组合能力**：通过顺序组合发现场景间交互产生的潜在 Bug，这是传统 BDD 难以企及的。

### 8. 不足与局限
*   **控制流限制**：目前的 DSL 主要支持顺序流，对于非确定性行为、并发执行或复杂的循环结构支持有限。
*   **适配器依赖**：虽然测试脚本是自动生成的，但将脚本连接到实际被测系统的“胶水代码”（Glue Code/Step Definitions）仍需手动编写。
*   **状态爆炸风险**：对于变量极多、逻辑极其复杂的系统，合成的主模型可能会面临状态爆炸问题，导致 SMT 求解效率下降。
*   **迁移成本**：从现有自由格式的 BDD 迁移到结构化更严谨的 PICKLES 语法仍需要一定的学习和重写成本。

（完）
