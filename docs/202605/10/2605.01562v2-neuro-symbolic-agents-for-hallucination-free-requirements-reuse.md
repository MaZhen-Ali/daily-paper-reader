---
title: Neuro-Symbolic Agents for Hallucination-Free Requirements Reuse
title_zh: 旨在实现无幻觉需求复用的神经符号智能体
authors: Ahmed F. Ibrahim
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01562v2"
tags: ["query:re-us"]
score: 8.0
evidence: 需求编写与管理框架 OOMRAM
tldr: "针对需求重用中传统方法过于僵化及大语言模型易产生幻觉的问题，本文提出一种神经符号多智能体系统。该系统将需求重用重新定义为模型驱动的启发过程，利用LLM作为启发式搜索工具遍历形式化的需求格结构，并结合确定性符号验证器强制执行结构约束。实验表明，该方法在保证100%需求覆盖率的同时，几乎消除了幻觉，确保了生成规范的结构合法性与合规性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01562v2/fig-001.webp\", \"caption\": \"Fig. 2. LangGraph agent workflow. The Navigator suggests the next discriminant node; the Interpreter (LLM) maps the vision to requirement IDs; the Validator (deterministic Python) checks constraints and may send the proposal back for revision; the Scribe compiles the final specification.\", \"page\": 3, \"index\": 1, \"width\": 529, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2605.01562v2/fig-002.webp\", \"caption\": \"Fig. 1. Sub-graph of the eRecordKeeping lattice. Core nodes (mandatory) are in blue; single adaptor node (choose exactly one) is in orange; its children are alternatives (grey); the option node is in green. The deterministic validator enforces these types at runtime.\", \"page\": 3, \"index\": 2, \"width\": 529, \"height\": 264}]"
motivation: 传统需求重用框架因匹配规则僵化难以跨场景适配，而引入LLM虽能提升灵活性却面临生成内容结构无效或不一致的幻觉风险。
method: 提出一种神经符号架构，由LLM负责在形式化需求格中进行非确定性启发式导航，并由符号验证器在循环中实时强制执行所有结构约束。
result: "在自动驾驶基准测试中实现了100%的需求覆盖率，约束违反率仅为0.2%，且所有生成的规范均符合结构合法性和强制性领域要求。"
conclusion: 该系统通过结合LLM的灵活性与符号逻辑的严谨性，为实现无幻觉、可审计且可扩展的需求重用提供了一种有效的模型驱动解决方案。
---

## 摘要
面向对象的需求编写与管理方法 (OOMRAM) 是一种需求复用框架，它依赖于精确的标识符匹配和僵化的模板，限制了其在不同语境下调整规范的能力。虽然大语言模型 (LLM) 提供了克服这一瓶颈的灵活性，但它们也带来了生成结构无效或不一致的需求组合的风险。为了解决这一矛盾，我们提出了一种神经符号多智能体系统，将需求复用重新概念化为模型驱动的引导过程。在这种范式中，LLM 作为一种非确定性启发式方法，用于遍历由形式化 OOMRAM 需求格表示的确定性领域模型。一个确定性的符号验证器在智能体循环中强制执行所有结构约束，从而通过构造有效地消除了幻觉需求组合。在跨两个应用系列的自主基准测试中，我们的系统实现了 100% 的需求覆盖率，且约束违反率仅为 0.2%。尽管针对单一金标准的 F1 分数适中 (0.47-0.51)，但生成的每个规范在结构上都是有效的，并满足所有强制性的领域需求。这种与模型无关的实现通过子图导航可扩展到更大的格，并为监管合规性提供透明的审计追踪。

## Abstract
The Object-Oriented Method for Requirements Authoring and Management (OOMRAM) is a requirements reuse framework that relies on exact identifier matching and rigid templates, limiting its ability to adapt specifications across diverse contexts. While Large Language Models (LLMs) offer the flexibility to overcome this bottleneck, they introduce the risk of generating structurally invalid or inconsistent requirement combinations. To address this tension, we present a neuro-symbolic multi-agent system that re-conceptualizes requirements reuse as a Model-Driven Elicitation process. In this paradigm, an LLM serves as a non-deterministic heuristic for traversing a deterministic domain model represented by a formal OOMRAM requirement lattice. A deterministic, symbolic validator enforces all structural constraints within the agent loop, effectively eliminating hallucinated requirement combinations by construction. Evaluated on an autonomous benchmark across two application families, our system achieves 100% requirement coverage and a constraint-violation rate of only 0.2%. Although the F1-score against a single gold standard is moderate (0.47-0.51), every generated specification is structurally valid and satisfies all mandatory domain requirements. The model-agnostic implementation scales to larger lattices via subgraph navigation and provides transparent audit trails for regulatory compliance.

---

## 论文详细总结（自动生成）

以下是对论文《Neuro-Symbolic Agents for Hallucination-Free Requirements Reuse》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：需求工程（RE）成本高且易出错，复用已验证的需求是提高效率的关键。**OOMRAM**（面向对象的需求编写与管理方法）是一种成熟的需求复用框架，它将需求组织为形式化的“格”（Lattice）结构。
*   **核心问题**：
    1.  **僵化性**：传统的 OOMRAM 依赖精确的标识符匹配，用户必须知道确切的 ID 才能检索需求，难以处理自然语言描述。
    2.  **幻觉风险**：虽然大语言模型（LLM）能理解自然语言，但在生成需求规范时经常产生“幻觉”，例如发明不存在的 ID、违反强制性约束或组合互斥的需求。
*   **研究目标**：结合 LLM 的灵活性与符号逻辑的严谨性，构建一个既能理解自然语言愿景，又能保证生成结果 100% 符合领域约束的自动化系统。

### 2. 方法论：核心思想与技术细节
该论文提出了一种**神经符号多智能体架构**，将需求复用视为一个“模型驱动的启发过程”：
*   **核心思想**：将 LLM 视为在确定性状态空间（需求格）中进行搜索的“非确定性启发式工具”，而由一个纯算法（非 LLM）的符号验证器担任“守门员”。
*   **关键技术细节**：
    *   **需求格形式化**：定义为有向无环图 $L=(V, E, T, \Phi)$，其中 $T$ 定义节点类型（核心、单适配器/XOR、多适配器/OR、可选），$\Phi$ 为布尔约束。
    *   **多智能体协同（基于 LangGraph）**：
        1.  **Navigator（导航员）**：负责深度优先遍历需求格，决定下一个要处理的决策点。
        2.  **Interpreter（解释员）**：由 LLM 驱动，根据项目愿景从当前子图中选择匹配的需求 ID。
        3.  **Validator（验证器）**：**纯 Python 编写的确定性算法**。它根据格定义的约束（如 XOR 必须且只能选一个）检查 Interpreter 的输出。若不合规，则将错误信息反馈给 Interpreter 进行修正。
        4.  **Scribe（记录员）**：汇总最终选定的 ID，生成格式化的规范文档。
    *   **子图导航策略**：为了处理大规模工业需求格，系统每次只向 LLM 展示当前节点的直接子节点，确保上下文窗口大小恒定（$O(1)$），使系统具备线性扩展性。

### 3. 实验设计
*   **场景与数据集**：
    *   使用了两个应用系列的需求格：**eRecordKeeping**（约 60 个需求节点）和 **SmartHome**（约 20 个需求节点）。
    *   设计了 **10 个项目愿景**（自然语言描述），涵盖从极简配置到复杂系统的不同需求。
*   **Benchmark（基准）**：
    *   由专家手动为每个愿景编写“金标准”需求集。
    *   评估指标包括：需求覆盖率、约束违反率、F1 分数、完成率以及推理延迟。
*   **对比方法**：主要对比了受约束的神经符号循环与不受约束（无验证器反馈）的纯 LLM 生成效果。

### 4. 资源与算力
*   **硬件设备**：实验在一台消费级机器上运行（**Apple M1 Pro 芯片，32 GB RAM**）。
*   **模型使用**：通过 **Ollama** 部署本地开源模型 **llama3.1:8b-instruct-q4_0**。
*   **参数设置**：LLM 温度（Temperature）设为 0 以保证输出的稳定性。
*   **运行耗时**：平均每个愿景的处理时间约为 210 秒，平均涉及 78 次 LLM 调用。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 10 组自主模拟实验（3 组详细对比金标准，7 组验证鲁棒性）。
*   **充分性评价**：
    *   **客观性**：实验跨越了两个不同领域的应用系列，证明了方法的通用性。
    *   **严谨性**：通过记录 LLM 的修正循环，展示了验证器在消除幻觉方面的必要性。
    *   **局限性**：实验样本量（10 个愿景）相对较小，且主要依赖单一的 llama3.1 模型，尚未在更大规模的工业级格（如数百个节点）上进行详尽的压力测试。

### 6. 主要结论与发现
*   **消除幻觉**：系统实现了 **100% 的结构合法性**。虽然在过程中 Interpreter 产生了 0.2% 的约束违反，但均被 Validator 拦截并引导修正，最终输出无幻觉。
*   **性能表现**：所有任务均 100% 完成。F1 分数在 0.47 到 0.81 之间。作者指出，较低的 F1 分数并非失败，而是因为需求格中存在大量合法的可选路径，系统生成的有效配置可能与人工金标准不同。
*   **“有意冲突”现象**：发现 LLM 有时为了满足用户“低成本”的愿景而试图忽略强制性的安全需求，而符号验证器成功强制保留了这些合规性要求。

### 7. 优点（亮点）
*   **架构创新**：将 LangGraph 的状态机特性与形式化方法结合，解决了 LLM 在严谨工程领域应用的核心痛点。
*   **确定性保证**：通过构造（By Construction）消除了幻觉，而非仅仅通过提示词工程（Prompt Engineering）来缓解。
*   **可扩展性**：子图导航策略解决了长文本上下文丢失（Lost-in-the-middle）的问题，支持处理大型工业模型。
*   **透明度**：提供了完整的审计追踪，符合监管对需求工程可追溯性的要求。

### 8. 不足与局限
*   **评估指标局限**：由于需求复用存在“多解性”，传统的 F1 分数难以完全体现系统生成的质量。
*   **延迟问题**：平均 210 秒的处理时间对于实时交互可能稍慢，且 LLM 调用次数较多，存在一定的计算开销。
*   **领域依赖**：系统的前提是必须先存在一个形式化的 OOMRAM 格，对于完全缺乏结构化知识的领域无法直接使用。
*   **人工参与度**：虽然实验是全自动的，但在实际应用中，如何更好地进行人机协作（Human-in-the-loop）尚需进一步探讨。

（完）
