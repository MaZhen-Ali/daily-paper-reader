---
title: Neuro-Symbolic Agents for Hallucination-Free Requirements Reuse
title_zh: 用于无幻觉需求复用的神经符号智能体
authors: Ahmed Ibrahim
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01562v1"
tags: ["query:re-us"]
score: 8.0
evidence: 需求编写和管理框架
tldr: "针对需求重用中传统方法过于僵化以及大语言模型易产生幻觉的问题，本文提出了一种神经符号多智能体系统。该系统将需求重用重新定义为模型驱动的启发式过程，利用LLM在确定的领域模型（OOMRAM格）中进行导航，并结合符号验证器强制执行结构约束。实验表明，该方法在保证100%需求覆盖率的同时，几乎消除了幻觉组合，为受监管行业的合规性提供了透明且有效的解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01562v1/fig-001.webp\", \"caption\": \"Fig. 1. Sub-graph of the eRecordKeeping lattice. Core nodes (mandatory) are in blue; single adaptor node (choose exactly one) is in orange; its children are alternatives (grey); the option node is in green. The deterministic validator enforces these types at runtime.\", \"page\": 3, \"index\": 1, \"width\": 529, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2605.01562v1/fig-002.webp\", \"caption\": \"Fig. 2. LangGraph agent workflow. The Navigator suggests the next discriminant node; the Interpreter (LLM) maps the vision to requirement IDs; the Validator (deterministic Python) checks constraints and may send the proposal back for revision; the Scribe compiles the final specification.\", \"page\": 3, \"index\": 2, \"width\": 529, \"height\": 237}]"
motivation: 传统需求重用框架因匹配规则僵化难以适应多样化语境，而大语言模型虽具灵活性却面临生成无效或不一致需求组合的风险。
method: 提出一种神经符号架构，将LLM作为非确定性启发式工具来遍历确定的需求格模型，并由符号验证器在循环中强制执行所有结构约束。
result: "在自动基准测试中实现了100%的需求覆盖率，约束违反率仅为0.2%，且所有生成的规范在结构上均有效并满足强制性领域要求。"
conclusion: 该系统通过结合LLM的灵活性与符号逻辑的严谨性，实现了无幻觉的需求重用，并为大规模、高合规性要求的领域提供了可扩展且可审计的解决方案。
---

## 摘要
面向对象的需求编写与管理方法（OOMRAM）是一种需求复用框架，它依赖于精确的标识符匹配和僵化的模板，限制了其在不同语境下调整规范的能力。虽然大语言模型（LLM）提供了克服这一瓶颈的灵活性，但它们也带来了生成结构无效或不一致的需求组合的风险。为了解决这一矛盾，我们提出了一种神经符号多智能体系统，将需求复用重新概念化为一种“模型驱动的引出过程”。在这种范式中，LLM 充当非确定性启发式算法，用于遍历由形式化 OOMRAM 需求格表示的确定性领域模型。一个确定性的符号验证器在智能体循环中强制执行所有结构约束，从而通过构造有效地消除了幻觉产生的需求组合。在跨越两个应用系列的自主基准测试中进行的评估显示，我们的系统实现了 100% 的需求覆盖率，且约束违反率仅为 0.2%。尽管针对单一金标准的 F1 分数适中（0.47–0.51），但生成的每个规范在结构上都是有效的，并满足所有强制性的领域需求。这种与模型无关的实现通过子图导航扩展到更大的格，并为监管合规性提供透明的审计追踪。

## Abstract
The Object-Oriented Method for Requirements Authoring and Management (OOMRAM) is a requirements reuse framework that relies on exact identifier matching and rigid templates, limiting its ability to adapt specifications across diverse contexts. While Large Language Models (LLMs) offer the flexibility to overcome this bottleneck, they introduce the risk of generating structurally invalid or inconsistent requirement combinations. To address this tension, we present a neuro-symbolic multi-agent system that re-conceptualizes requirements reuse as a \textbf{Model-Driven Elicitation process}. In this paradigm, an LLM serves as a \textbf{non-deterministic heuristic} for traversing a \textbf{deterministic domain model} represented by a formal OOMRAM requirement lattice. A deterministic, symbolic validator enforces all structural constraints within the agent loop, effectively eliminating hallucinated requirement combinations by construction. Evaluated on an autonomous benchmark across two application families, our system achieves 100\% requirement coverage and a constraint-violation rate of only 0.2\%. Although the F1-score against a single gold standard is moderate (0.47--0.51), every generated specification is structurally valid and satisfies all mandatory domain requirements. The model-agnostic implementation scales to larger lattices via subgraph navigation and provides transparent audit trails for regulatory compliance.

---

## 论文详细总结（自动生成）

### 论文总结：用于无幻觉需求复用的神经符号智能体

#### 1. 论文的核心问题与整体含义
在软件需求工程（RE）中，**需求复用**是提高效率的关键。传统方法如 **OOMRAM**（面向对象的需求编写与管理方法）依赖于精确的标识符匹配，过于僵化，无法处理自然语言描述。而大语言模型（LLM）虽然具备极高的灵活性，但在处理复杂逻辑约束时极易产生**“幻觉”**（如发明不存在的需求 ID、违反互斥约束、遗漏强制性需求）。
本文的核心问题是如何在利用 LLM 的自然语言理解能力的同时，确保生成的复用需求规范在逻辑和结构上绝对正确。

#### 2. 方法论：核心思想与技术细节
论文提出了一种**神经符号（Neuro-Symbolic）多智能体架构**，将需求复用重新定义为“模型驱动的引出过程”：
*   **核心思想**：将 LLM 视为在确定性搜索空间（需求格模型）中进行导航的“非确定性启发式工具”，而由符号逻辑（Python 代码）实现的验证器则充当“硬性护栏”。
*   **关键技术细节**：
    *   **需求格（Lattice）表示**：将应用家族定义为有向无环图（DAG），节点分为核心（Core）、单适配器（SA，即 XOR）、多适配器（MA，即 OR）和选项（Option）。
    *   **多智能体工作流（基于 LangGraph）**：
        1.  **Navigator（导航员）**：决定遍历路径，采用子图导航策略以处理大规模模型。
        2.  **Interpreter（解释员）**：由 LLM 驱动，将用户愿景映射为具体的需求 ID。
        3.  **Validator（验证员）**：**纯符号逻辑（无 LLM）**，根据格模型的布尔约束检查 Interpreter 的输出。若发现违反约束（如 XOR 节点选了两个子项），则打回重做。
        4.  **Scribe（记录员）**：编译最终的规范文档。
*   **算法流程**：系统通过“解释-验证-修正”的循环，直到所有选择均满足全局一致性谓词（满足基数约束和父子结构完整性）。

#### 3. 实验设计
*   **数据集/场景**：
    *   **eRecordKeeping**：包含约 60 个需求的形式化格模型。
    *   **SmartHome**：包含约 20 个需求的合成模型。
    *   此外还构建了一个包含 12 个领域的语料库（如汽车 IVI、核心银行、电信等）用于验证扩展性。
*   **Benchmark**：针对 10 个自然语言项目愿景（Vision），由专家手动编写“金标准”需求集。
*   **对比方法**：
    *   **消融实验**：对比了“全智能体系统”与“纯 LLM 生成（无验证器护栏）”的表现。

#### 4. 资源与算力
*   **硬件**：实验在消费级设备上完成（Apple M1 Pro 芯片，32 GB RAM）。
*   **模型**：使用 **Ollama** 在本地部署 **Llama 3.1:8B-instruct**（Q4_0 量化版）。
*   **参数设置**：Temperature 设为 0 以保证确定性采样。
*   **耗时**：平均每个愿景的处理延迟约为 210 秒，平均调用 LLM 78 次。

#### 5. 实验数量与充分性
*   **实验规模**：对 10 个项目愿景进行了自主模拟测试，并对其中 3 个具有完整金标准的愿景进行了深度指标分析。
*   **充分性**：实验涵盖了不同复杂度的领域（从简单的智能家居到复杂的电信系统），并进行了消融实验来验证验证器的必要性。
*   **客观性**：通过本地模型和 0 温度设置确保了结果的可重复性。虽然样本量（10 个愿景）在统计学上不算巨大，但对于验证“结构正确性”这一核心目标而言是充分的。

#### 6. 主要结论与发现
*   **100% 结构有效性**：所有生成的规范均通过了符号验证，完全符合 OOMRAM 的形式化约束。
*   **极低的违反率**：在运行过程中，约束违反率仅为 0.2%，且所有违反均在最终输出前被验证器拦截并修正。
*   **F1 分数的误导性**：实验获得的 F1 分数在 0.47 到 0.81 之间。作者指出，在产品线工程中，有效的配置空间很大，偏离单一金标准并不代表失败，只要满足结构有效性即可。
*   **消融结果**：移除验证器后，纯 LLM 的有效性骤降至 14%，平均每个规范包含 5.1 个错误。

#### 7. 优点
*   **彻底消除幻觉**：通过符号验证器从构造上保证了输出的逻辑严密性，解决了 LLM 在严肃工程领域应用的最大痛点。
*   **可扩展性**：子图导航策略使得系统能够处理大规模工业级需求格，而不会受限于 LLM 的上下文窗口。
*   **透明与合规**：提供完整的审计追踪，适合安全关键型（Safety-Critical）和高监管行业。

#### 8. 不足与局限
*   **前置成本**：系统高度依赖于预先构建的高质量 OOMRAM 需求格，将非结构化遗留数据转化为这种格模型的过程尚未自动化。
*   **延迟较高**：由于采用了细粒度的“微决策”循环，完成一次需求引出需要多次 LLM 调用，实时交互性有待提升。
*   **模型依赖**：虽然框架是模型无关的，但 Interpreter 的表现仍受限于底层 LLM 的推理能力（实验仅使用了 8B 模型）。
*   **评估指标**：传统的 NLP 指标（如 F1）难以准确衡量此类系统的优劣，需要建立更符合产品线工程的评估体系。

（完）
