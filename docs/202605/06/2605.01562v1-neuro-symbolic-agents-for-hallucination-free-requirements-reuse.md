---
title: Neuro-Symbolic Agents for Hallucination-Free Requirements Reuse
title_zh: 用于无幻觉需求复用的神经符号智能体
authors: Ahmed Ibrahim
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01562v1"
tags: ["query:re-us"]
score: 9.0
evidence: 需求编写、管理和模型驱动的需求获取
tldr: "针对需求重用中传统方法过于僵化以及大语言模型易产生幻觉的问题，本文提出了一种神经符号多智能体系统。该系统将需求重用重新定义为模型驱动的启发式过程，利用大模型作为启发式搜索工具，在确定性的领域模型格结构中进行导航，并通过符号验证器强制执行结构约束。实验表明，该方法在保证100%需求覆盖率的同时，几乎消除了幻觉，为受监管行业的合规性提供了透明的审计追踪。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01562v1/fig-001.webp\", \"caption\": \"Fig. 1. Sub-graph of the eRecordKeeping lattice. Core nodes (mandatory) are in blue; single adaptor node (choose exactly one) is in orange; its children are alternatives (grey); the option node is in green. The deterministic validator enforces these types at runtime.\", \"page\": 3, \"index\": 1, \"width\": 529, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2605.01562v1/fig-002.webp\", \"caption\": \"Fig. 2. LangGraph agent workflow. The Navigator suggests the next discriminant node; the Interpreter (LLM) maps the vision to requirement IDs; the Validator (deterministic Python) checks constraints and may send the proposal back for revision; the Scribe compiles the final specification.\", \"page\": 3, \"index\": 2, \"width\": 529, \"height\": 237}]"
motivation: 传统需求重用框架因匹配规则过于僵化而受限，而大语言模型在生成需求时容易产生结构无效或不一致的幻觉。
method: 提出一种神经符号架构，将大模型作为非确定性启发式工具，在受符号验证器约束的确定性需求格模型中进行导航与启发式搜索。
result: "在自动基准测试中实现了100%的需求覆盖率，且约束违规率仅为0.2%，确保了所有生成的规格在结构上均有效。"
conclusion: 该系统通过结合大模型的灵活性与符号逻辑的严谨性，实现了无幻觉的需求重用，并为复杂领域的合规性提供了可扩展且透明的解决方案。
---

## 摘要
面向对象的需求编写与管理方法 (OOMRAM) 是一种需求复用框架，它依赖于精确的标识符匹配和僵化的模板，限制了其在不同语境下调整规范的能力。虽然大语言模型 (LLM) 提供了克服这一瓶颈的灵活性，但它们也带来了生成结构无效或不一致的需求组合的风险。为了解决这一矛盾，我们提出了一种神经符号多智能体系统，将需求复用重新概念化为一种模型驱动的引出过程。在这种范式中，LLM 作为一种非确定性启发式方法，用于遍历由形式化 OOMRAM 需求格表示的确定性领域模型。一个确定性的符号验证器在智能体循环中强制执行所有结构约束，从而通过构造有效地消除了幻觉需求组合。在跨越两个应用系列的自主基准测试中，我们的系统实现了 100% 的需求覆盖率，且约束违反率仅为 0.2%。尽管针对单一金标准的 F1 分数适中（0.47--0.51），但生成的每一份规范在结构上都是有效的，并满足所有强制性的领域需求。这种与模型无关的实现通过子图导航扩展到更大的格，并为监管合规性提供透明的审计追踪。

## Abstract
The Object-Oriented Method for Requirements Authoring and Management (OOMRAM) is a requirements reuse framework that relies on exact identifier matching and rigid templates, limiting its ability to adapt specifications across diverse contexts. While Large Language Models (LLMs) offer the flexibility to overcome this bottleneck, they introduce the risk of generating structurally invalid or inconsistent requirement combinations. To address this tension, we present a neuro-symbolic multi-agent system that re-conceptualizes requirements reuse as a \textbf{Model-Driven Elicitation process}. In this paradigm, an LLM serves as a \textbf{non-deterministic heuristic} for traversing a \textbf{deterministic domain model} represented by a formal OOMRAM requirement lattice. A deterministic, symbolic validator enforces all structural constraints within the agent loop, effectively eliminating hallucinated requirement combinations by construction. Evaluated on an autonomous benchmark across two application families, our system achieves 100\% requirement coverage and a constraint-violation rate of only 0.2\%. Although the F1-score against a single gold standard is moderate (0.47--0.51), every generated specification is structurally valid and satisfies all mandatory domain requirements. The model-agnostic implementation scales to larger lattices via subgraph navigation and provides transparent audit trails for regulatory compliance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Neuro-Symbolic Agents for Hallucination-Free Requirements Reuse》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：需求工程（RE）成本高且易出错，复用已验证的需求是提高效率的关键。传统的 OOMRAM（面向对象的需求编写与管理方法）框架依赖精确的 ID 匹配，过于僵化，无法处理自然语言描述。
*   **核心矛盾**：虽然大语言模型（LLM）具备理解自然语言的灵活性，但其生成的输出具有随机性，容易产生“幻觉”（如发明不存在的需求 ID、违反逻辑约束、组合互斥选项），这在安全关键型领域是不可接受的。
*   **整体含义**：论文旨在结合 LLM 的灵活性与形式化方法的严谨性，实现一种既能理解用户愿景又能保证逻辑 100% 正确的需求复用系统。

### 2. 方法论：神经符号多智能体系统
*   **核心思想**：将需求复用重新定义为“模型驱动的引出（Model-Driven Elicitation）”过程。LLM 被视为在确定性领域模型（需求格结构）中进行搜索的“非确定性启发式工具”，而符号验证器则作为硬性约束闸门。
*   **关键技术细节**：
    *   **需求格（Lattice）表示**：将需求族建模为有向无环图（DAG），定义了核心需求（Core）、单选适配器（SA/XOR）、多选适配器（MA/OR）和可选需求（Op）等类型。
    *   **多智能体架构（基于 LangGraph）**：
        *   **Navigator（导航员）**：决定遍历路径，管理子图导航以处理大规模模型。
        *   **Interpreter（解释员）**：LLM 核心，将用户愿景映射为具体的候选需求 ID。
        *   **Validator（验证器）**：**纯 Python 编写的确定性算法**（不含 LLM），根据格约束（如 XOR 必须且只能选一个）检查 Interpreter 的输出。
        *   **Scribe（记录员）**：编译最终的规范文档。
    *   **自校正循环**：如果 Validator 发现违规，会将错误信息反馈给 Interpreter 进行修正，直到满足所有约束。

### 3. 实验设计与基准测试
*   **数据集/场景**：构建了一个包含 12 个领域的语料库，包括 eRecordKeeping（57 个需求）、SmartHome、汽车信息娱乐（ISO 26262）、核心银行（ISO 20022）、电信 OSS/BSS（49 个需求）等。
*   **Benchmark**：针对 10 个项目愿景（Vision）进行自主模拟。
*   **对比方法**：
    *   **Gold Standard 对比**：与人工编写的“金标准”需求集对比 F1 分数。
    *   **消融实验**：对比了“全智能体集群”与“纯 LLM 生成（无验证器）”的表现。

### 4. 资源与算力
*   **硬件环境**：实验在消费级机器上完成（Apple M1 Pro，32 GB RAM）。
*   **模型部署**：使用 **Ollama** 本地托管的 `llama3.1:8b-instruct-q4_0` 量化模型。
*   **运行参数**：Temperature 设为 0（确保确定性），平均每个愿景调用 LLM 约 78 次，平均耗时 210 秒。

### 5. 实验数量与充分性
*   **实验规模**：详细测试了 3 个具有完整金标准的愿景，并对另外 7 个愿景进行了鲁棒性验证。此外，还针对 12 个不同复杂度的领域进行了约束类型分析。
*   **充分性评价**：实验设计较为充分，涵盖了从安全关键（汽车）到高度监管（银行）的多种场景。消融实验客观地证明了符号验证器的必要性。不过，由于金标准由作者手动指定，样本量（10 个愿景）对于大规模统计推断来说仍显略小。

### 6. 主要结论与发现
*   **消除幻觉**：系统实现了 **100% 的结构有效率**。虽然 LLM 在初始阶段会产生错误，但验证器循环将最终违规率降至 **0.2%**（且均在输出前被修正）。
*   **覆盖率与有效性**：实现了 100% 的需求覆盖。F1 分数（0.47-0.81）虽然中等，但作者指出这是因为需求复用存在多种合法的可选组合，而非系统错误。
*   **可扩展性**：通过子图导航策略，系统能够处理大型工业级需求格（如电信领域），且推理质量不会随模型增大而下降。

### 7. 优点与亮点
*   **架构创新**：成功将 LangGraph 的智能体流与形式化逻辑验证结合，解决了 LLM 在严谨工程领域的落地难题。
*   **合规性透明**：系统提供完整的审计追踪（Audit Trails），每一步选择都有据可查，符合监管要求。
*   **模型无关性**：支持本地部署的小规模模型（如 8B 参数），无需依赖昂贵的云端 API，保护了企业隐私。

### 8. 不足与局限
*   **评估指标局限**：传统的 NLP 指标（如 F1 分数）在衡量具有多种合法解的需求配置时可能失效，需要更科学的 PLE（产品线工程）评估标准。
*   **前置成本**：系统依赖于预先构建的 OOMRAM 需求格。将非结构化的原始文档转化为这种形式化格结构的过程（领域工程）在文中未详细讨论，这通常是实际应用中的主要瓶颈。
*   **延迟问题**：由于引入了多次 LLM 调用和自校正循环，生成一份规范需要数分钟，对于实时性要求极高的场景可能不适用。

（完）
