---
title: "PICKLES: a Natural Language Framework for Requirement Specification and Model-Based Testing"
title_zh: PICKLES：一种用于需求规范和基于模型测试的自然语言框架
authors: "María Belén Rodríguez, Petra van den Bos"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26572v1"
tags: ["query:re-us"]
score: 9.0
evidence: Gherkin风格的BDD场景和需求规范
tldr: PICKLES 框架结合了行为驱动开发（BDD）的可读性与基于模型测试（MBT）的自动化优势。它引入了一种扩展 Gherkin 的结构化自然语言 PICKLES，支持将测试场景双向转换为形式化模型。通过整合多个场景生成主模型，该框架能自动派生测试用例。在工业级交通管理系统案例中，PICKLES 在相同场景下比传统 BDD 显著提升了测试覆盖率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26572v1/fig-001.webp\", \"caption\": \"Figure 3 Smaster. Each line style (dashed, dotted, solid, dash-dotted) denotes transitions from S1–S4. Variables follow the reference in Table 1.\", \"page\": 10, \"index\": 1, \"width\": 1021, \"height\": 549}, {\"url\": \"assets/figures/arxiv/2604.26572v1/fig-002.webp\", \"caption\": \"Figure 2 STS1: Faulty detectors outside the critical section.\", \"page\": 8, \"index\": 2, \"width\": 520, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.26572v1/fig-003.webp\", \"caption\": \"Figure 4 Coverage of STSmaster. Transitions and states covered by only executing the specification scenarios are shown in black, whereas transitions in red show non-tested transitions.\", \"page\": 12, \"index\": 3, \"width\": 509, \"height\": 367}]"
motivation: 旨在解决 BDD 缺乏自动化测试生成能力以及 MBT 模型难以被非技术人员理解和维护的问题。
method: 提出 PICKLES 语言并实现其与形式化模型的双向转换，通过合成主模型并应用标准 MBT 算法自动生成测试。
result: 工业案例研究表明，该方法在保持规范可读性的同时，比传统 BDD 实现了显著更高的测试覆盖率。
conclusion: PICKLES 证明了利用结构化自然语言进行形式化建模和自动化测试在工业实践中的可行性与优越性。
---

## 摘要
本文结合了基于模型测试（MBT）和行为驱动开发（BDD）领域的方法，定义了一种测试方法，该方法既具有 BDD 中人类可读的规范和测试用例，又利用了 MBT 的建模技术和自动测试生成算法。我们引入了 PICKLES（一种用于测试场景的精确输入和控制流基于关键词的语言），它是 Gherkin 风格 BDD 场景的扩展，采用结构化自然语言进行规范。我们提供了从 Pickles 场景到形式化模型的双向转换，使规范和测试均保持人类可读，并提出了一种获取所谓“主模型”的方法，该模型整合了所有转换后的场景。随后可以应用标准的 MBT 算法从中自动导出测试用例。我们实现了转换和组合步骤的原型，并将其应用于一个工业案例研究：交通管理系统中的一个软件组件。通过该案例，我们展示了整个工作流，并证明了 Pickles 如何在相同场景集下实现比 BDD 显著更高的覆盖率。

## Abstract
This paper combines methods from the fields of Model-Based Testing (MBT) and Behaviour-Driven Development (BDD) to define a testing approach with human-readable specifications and test cases, as in BDD, while using the modelling techniques and automatic test generation algorithms from MBT. We introduce PICKLES, a Precise Input and Control-flow Keyword-based Language for tEst Scenarios; an extension of Gherkin-style BDD scenarios, specified in structured natural language. We provide a bi-directional translation from Pickles scenarios to formal models, where both specifications and tests are human-readable, and a method to obtain a so-called master model combining all translated scenarios. Standard MBT algorithms can then be applied to automatically derive test cases from it. We implement a prototype of the translation and composition steps, which we use on an industrial case study: a software component from a traffic management system. With it, we illustrate the pipeline and show how Pickles can achieve significantly higher coverage with respect to BDD from the same set of scenarios.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **PICKLES** 的框架，旨在弥合行为驱动开发（BDD）与基于模型测试（MBT）之间的鸿沟。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在工业界，测试面临“易读性”与“严谨性”的权衡。
    *   **BDD（如 Gherkin/Cucumber）**：使用自然语言，易于非技术利益相关者理解，但缺乏严谨性，且测试用例通常是孤立的示例，难以实现高覆盖率。
    *   **MBT（基于模型测试）**：能够自动生成高覆盖率的测试，但形式化模型编写困难、维护成本高，且模型对非专家而言不可读。
*   **研究动机**：开发一种既能保持 BDD 自然语言可读性，又能利用 MBT 自动化生成和严谨逻辑能力的测试框架。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：引入一种结构化的自然语言 **PicklesDSL**，将其作为需求规范和测试用例的统一载体，并通过自动化的双向转换连接形式化模型。
*   **关键技术细节**：
    *   **PicklesDSL**：在传统 Gherkin（Given-When-Then）基础上增加了变量设置块（Variable Settings），允许定义变量类型（原始类型、数组、结构体）及其取值范围（Range）。
    *   **双向转换**：
        *   **正向**：将 Pickles 场景转换为**符号迁移系统（STS）**。Given 映射为前置条件/初始守卫，When 映射为输入迁移，Then 映射为输出迁移。
        *   **反向**：将 MBT 算法生成的形式化测试路径转换回人类可读的 Pickles 测试用例。
    *   **主模型合成（Master Model Composition）**：
        *   **选择组合（Choice Composition, $\biguplus$）**：合并具有相同初始状态的场景。
        *   **顺序组合（Sequential Composition, $\rhd$）**：允许场景之间链式执行。
        *   通过合成，将孤立的 BDD 场景转化为一个能够表达复杂交互逻辑的统一模型。

### 3. 实验设计
*   **实验场景**：使用了一个真实的工业案例研究——荷兰 Technolution B.V. 公司的交通管理系统组件。该组件负责根据传感器数据检测道路异常（如低速或停车），并根据是否处于“关键路段”更新 UI 状态。
*   **Benchmark（基准）**：
    *   原始的 BDD 测试套件（基于 Cucumber 实现）。
    *   手动从原始测试中提取的 4 个核心场景。
*   **对比方法**：对比了“传统 BDD 执行（孤立执行场景）”与“PICKLES 框架执行（基于主模型生成测试）”在覆盖率上的表现。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号或大规模算力消耗。
*   **实现细节**：该原型工具使用 Python 开发，利用 `Lark` 库进行语法解析，并生成 JSON 格式的 STS 模型。由于该方法属于形式化方法和逻辑推理范畴，而非深度学习，其计算开销主要集中在 SMT 求解器（用于路径约束求解）和模型合成上，通常在普通 CPU 上即可运行。

### 5. 实验数量与充分性
*   **实验规模**：主要围绕 1 个深度工业案例展开，包含 4 个复杂的参数化场景。
*   **充分性评价**：
    *   **深度充分**：通过一个真实的、具有复杂数据结构（数组、嵌套结构体）的工业组件验证了端到端的流程。
    *   **广度有限**：作为一项探索性研究，实验仅限于单一案例，尚未在多种不同类型的系统（如高并发系统或非确定性系统）上进行大规模基准测试。
    *   **客观性**：实验对比了转换覆盖率和输入空间覆盖率，提供了量化的改进数据（如覆盖率从 30% 提升到 100%），论证较为客观。

### 6. 主要结论与发现
*   **覆盖率显著提升**：在相同的初始场景定义下，PICKLES 通过模型合成将迁移覆盖率（Transition Coverage）从 30% 提升至 100%。
*   **输入空间扩展**：通过参数化定义而非硬编码示例，PICKLES 能够覆盖更广的输入域（案例中输入覆盖率提升了 98.5%），减少了测试者的确认偏差。
*   **可维护性增强**：减少了手动编写重复测试用例的需求，需求变更时只需修改 PicklesDSL 规范即可自动更新所有测试。

### 7. 优点
*   **双向透明性**：测试用例以自然语言生成，非技术人员也能看懂 MBT 生成的复杂测试路径。
*   **无缝集成**：生成的 Pickles 测试用例兼容现有的 BDD 工具链（如 Cucumber），降低了企业采纳 MBT 的门槛。
*   **形式化严谨**：通过 STS 和 SMT 求解，确保了自然语言背后的逻辑一致性。

### 8. 不足与局限
*   **手动适配器**：虽然测试用例是自动生成的，但将测试步骤连接到实际代码的“测试适配器（Glue Code）”仍需手动编写。
*   **非确定性支持**：目前的 DSL 和合成逻辑主要针对确定性行为，对复杂的非确定性系统支持有限。
*   **迁移成本**：从现有自由格式的自然语言需求迁移到结构化的 PicklesDSL 仍需要一定的人工泛化工作。
*   **规模化验证**：尚未在具有成百上千个场景的大型复杂系统中验证模型合成的性能瓶颈。

（完）
