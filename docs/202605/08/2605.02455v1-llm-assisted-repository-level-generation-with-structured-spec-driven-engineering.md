---
title: LLM-Assisted Repository-Level Generation with Structured Spec-Driven Engineering
title_zh: 基于结构化规范驱动工程的 LLM 辅助仓库级生成
authors: "Shuzhao Feng, Boqi Chen, Brett H Meyer, Gunter Mussbacher"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02455v1"
tags: ["query:re-us"]
score: 8.0
evidence: 结构化规范驱动的仓库级生成工程
tldr: 本研究针对大模型在仓库级代码生成中因自然语言歧义导致的质量瓶颈，提出了结构化规范驱动工程（SSDE）范式。该方法通过引入结构化制品作为输入，增强了生成过程的可验证性与准确性。通过对MVC架构业务逻辑的试点研究，展示了SSDE在处理复杂系统时的潜力，并为未来自动化软件工程提供了明确的路线图。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02455v1/fig-001.webp\", \"caption\": \"Figure 2: Overview of the Average Test Pass Rate\", \"page\": 3, \"index\": 1, \"width\": 506, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2605.02455v1/fig-002.webp\", \"caption\": \"Table 2: Test Pass Rate of the Generated Business Logic with Claude Sonnet 4.5 using Various Input Configurations\", \"page\": 4, \"index\": 2, \"width\": 1044, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2605.02455v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of SSDE Applied to Pilot Study\", \"page\": 2, \"index\": 3, \"width\": 506, \"height\": 411}]"
motivation: 大模型在处理仓库级代码生成时，因自然语言提示词的歧义性和缺乏可验证性导致输出质量显著下降。
method: 提出结构化规范驱动工程（SSDE），利用结构化制品引导大模型生成代码，并针对MVC业务逻辑进行试点研究。
result: 通过对三个软件系统和五种大模型的实验，验证了结构化规范在提升仓库级代码生成质量方面的可行性与潜力。
conclusion: SSDE范式通过提高输入的可验证性，为实现高质量、仓库级的自动化代码生成提供了切实可行的路径。
---

## 摘要
最先进的大语言模型（LLMs）在函数级代码生成方面表现出色。然而，当扩展到仓库级系统时，输出质量显著下降。目前仅依赖自然语言提示的工作流存在固有的歧义性且缺乏可验证性。为解决这一问题，我们提出了结构化规范驱动工程（SSDE），这是一种利用结构化产物来引导 LLM 生成的范式。我们认为，将结构化规范作为 LLM 输入使高质量的仓库级代码生成成为一个切实可行的目标，同时提供了卓越的可验证性，从而具有巨大的改进潜力。我们首先通过一项初步研究调查了这一愿景的可行性，该研究使用五种 LLM 为三个软件系统生成模型-视图-控制器（MVC）业务逻辑，随后强调了 SSDE 的潜力、挑战和未来路线图。

## Abstract
State-of-the-art Large Language Models (LLMs) excel in code generation at the function level. However, the output quality significantly declines when scaling to repository-level systems. Current workflows relying only on natural language prompts suffer from inherent ambiguity and a lack of verifiability. To address this, we propose structured spec-driven engineering (SSDE), a paradigm that leverages structured artifacts to guide LLM generation. We argue that structured specifications as LLM inputs make high-quality, repository-level code generation a tangible goal, while at the same time offering superior verifiability, leading to significant potential for improvement. We first investigate the feasibility of this vision through a pilot study generating Model-View-Controller (MVC) business logic for three software systems using five LLMs, and then highlight the potential, challenges, and future roadmap for SSDE.

---

## 论文详细总结（自动生成）

这篇论文由麦吉尔大学和渥太华大学的研究团队撰写，探讨了如何利用结构化规范提升大语言模型（LLM）在仓库级代码生成中的表现。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管 LLM 在函数级（Function-level）代码生成上表现优异，但在处理复杂的**仓库级（Repository-level）**系统时，质量显著下降。
*   **研究动机**：传统的自然语言（NL）提示词具有固有的**歧义性**且**难以验证**。增加自然语言的篇幅并不能解决精度缺失的问题。
*   **核心主张**：引入软件工程中成熟的**结构化规范（Structured Specifications）**（如 Gherkin 语言、领域模型）作为 LLM 的输入，可以提供更精确的指令并增强输出的可验证性，从而实现高质量的自动化仓库级开发。

### 2. 方法论：结构化规范驱动工程（SSDE）
*   **核心思想**：将 SSDE 定义为一种利用结构化产物引导 LLM 生成代码的范式。
*   **关键技术细节**：
    *   **输入组合**：研究对比了四种输入：
        1.  **自然语言规范**：自由格式的功能描述。
        2.  **Gherkin 规范**：使用 Given-When-Then 格式的结构化场景描述，具有可执行性。
        3.  **领域模型（Domain Model）**：使用 Umple 或 Ecore 建模，捕捉领域概念及其关系。
        4.  **签名模型（Signature Model）**：从领域模型生成的 API 接口定义（类和函数签名）。
    *   **生成流程**：LLM 接收上述规范及控制器模板，生成 Python MVC 架构中的业务逻辑（Controller 层），并与自动生成的模型层（Model 层）交互形成完整后端。
    *   **验证机制**：利用结构化规范的特性，通过静态分析和动态测试（单元测试）来验证生成代码的正确性。

### 3. 实验设计
*   **实验场景**：生成三个不同领域的 MVC 系统（Symboleo, CheECSEManager, MeetingGroups）的业务逻辑。
*   **Benchmark**：使用人工编写并验证过的 Python 单元测试套件作为基准，每个测试用例对应一个 Gherkin 场景。
*   **对比方法**：
    *   **模型对比**：对比了 Claude 4.5 Sonnet, Qwen 3 Coder (480B), GPT 5.1, GPT 5 Nano, Llama 3.2 (3B)。
    *   **配置对比**：对比了“仅自然语言”、“自然语言+模型”、“Gherkin+模型”等多种输入组合。
*   **评估指标**：测试通过率（Test Pass Rate, TPR）和定性错误分析。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件算力（如 GPU 型号或训练时长）。
*   **模型调用**：实验主要通过 API 或推理框架调用现有的 SOTA 模型。
*   **参数设置**：Claude、Qwen、Llama 使用 0.5 的温度系数（Temperature）；GPT 模型使用“中等”推理努力（Reasoning Effort）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 个软件系统、2 种建模工具（Umple/Ecore）、5 种 LLM。
    *   每种配置重复实验 **10 次**以减少随机性偏差。
    *   总计测试了 30 种主要的配置组合（5 LLM × 2 工具 × 3 样本）。
*   **充分性评价**：实验设计较为客观，涵盖了从超大规模模型到轻量级模型的对比，并引入了重复实验来评估 LLM 的随机性。虽然样本系统数量（3个）较少，但作为“试点研究（Pilot Study）”已具备足够的说服力。

### 6. 主要结论与发现
*   **结构化胜过自然语言**：加入任何形式的结构化规范都能显著提升生成质量。
*   **签名模型最有效**：提供生成的 API 签名（Signature Model）比直接提供原始领域模型效果更好（TPR 提升约 7.8%），因为 LLM 对原生代码的理解优于建模语言。
*   **错误类型分析**：超过 **70% 的生成错误**（如调用不存在的 API、类型不匹配）可以通过静态代码分析检测出来。
*   **模型差异**：Claude 4.5 Sonnet 在多数任务中表现最优，部分配置下达到了 100% 的测试通过率。

### 7. 优点
*   **范式创新**：将传统的模型驱动工程（MDE）与现代 LLM 结合，提出了 SSDE 这一具有前瞻性的工作流。
*   **可验证性强**：强调了结构化规范在自动化反馈循环（Feedback Loop）中的核心作用。
*   **路线图清晰**：不仅展示了实验结果，还为未来如何改进输出质量、评估现实影响和扩展适用性制定了详细路线图。

### 8. 不足与局限
*   **样本量有限**：仅使用了三个中小型 MVC 系统，对于超大规模、非 MVC 架构或遗留系统的适用性尚待验证。
*   **维护成本**：编写和维护结构化规范（如 Gherkin 和 UML）本身需要人工成本，论文尚未量化这种成本与手动编码成本的对比（即净生产力增益）。
*   **领域知识缺失**：实验发现 LLM 对特定建模工具（如 Umple）的语法理解仍有欠缺，导致性能波动。
*   **动态演进挑战**：目前的研究主要针对单次生成，尚未解决规范演进时如何进行局部代码更新（Partial Updates）的问题。

（完）
