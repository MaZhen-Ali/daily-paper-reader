---
title: "PICKLES: a Natural Language Framework for Requirement Specification and Model-Based Testing"
title_zh: PICKLES：一种用于需求规范和基于模型测试的自然语言框架
authors: "María Belén Rodríguez, Petra van den Bos"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26572v1"
tags: ["query:re-us"]
score: 9.0
evidence: 扩展Gherkin风格的BDD场景用于需求规范
tldr: PICKLES是一个结合了行为驱动开发（BDD）的可读性与基于模型测试（MBT）自动化能力的框架。它引入了一种扩展自Gherkin的结构化自然语言，实现了需求场景与形式化模型之间的双向转换，并能通过构建主模型自动生成高覆盖率的测试用例。该框架在工业级交通管理系统中得到了验证，证明了其在保持规格说明易读性的同时，能显著提升测试效率和覆盖率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26572v1/fig-001.webp\", \"caption\": \"Figure 3 Smaster. Each line style (dashed, dotted, solid, dash-dotted) denotes transitions from S1–S4. Variables follow the reference in Table 1.\", \"page\": 10, \"index\": 1, \"width\": 1021, \"height\": 549}, {\"url\": \"assets/figures/arxiv/2604.26572v1/fig-002.webp\", \"caption\": \"Figure 2 STS1: Faulty detectors outside the critical section.\", \"page\": 8, \"index\": 2, \"width\": 520, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.26572v1/fig-003.webp\", \"caption\": \"Figure 4 Coverage of STSmaster. Transitions and states covered by only executing the specification scenarios are shown in black, whereas transitions in red show non-tested transitions.\", \"page\": 12, \"index\": 3, \"width\": 509, \"height\": 367}]"
motivation: 旨在解决BDD在测试生成自动化程度上的不足，同时保留其需求规格说明在团队协作中的高可读性。
method: 提出了PICKLES语言并实现其与形式化模型的双向转换，通过构建整合场景的主模型并应用标准MBT算法自动导出测试用例。
result: 在工业级交通管理系统的案例研究中，PICKLES在相同场景集下实现了比传统BDD显著更高的测试覆盖率。
conclusion: PICKLES成功融合了BDD与MBT的优势，为需求规格说明和自动化模型测试提供了一种高效、易读且具备工业应用潜力的解决方案。
---

## 摘要
本文结合了基于模型测试（MBT）和行为驱动开发（BDD）领域的方法，旨在定义一种测试方法，该方法既能像 BDD 一样提供人类可读的规范和测试用例，又能利用 MBT 的建模技术和自动测试生成算法。我们引入了 PICKLES（一种用于测试场景的精确输入和控制流关键字语言），它是对以结构化自然语言编写的 Gherkin 风格 BDD 场景的扩展。我们提供了 Pickles 场景与形式化模型之间的双向转换方法，使规范和测试均保持人类可读，并提出了一种获取所谓“主模型”（master model）的方法，该模型整合了所有转换后的场景。随后，可以应用标准的 MBT 算法从中自动导出测试用例。我们实现了转换和组合步骤的原型，并将其应用于一个工业案例研究：交通管理系统中的一个软件组件。通过该案例，我们展示了完整的工作流，并证明了在相同场景集下，Pickles 相比 BDD 能实现显著更高的覆盖率。

## Abstract
This paper combines methods from the fields of Model-Based Testing (MBT) and Behaviour-Driven Development (BDD) to define a testing approach with human-readable specifications and test cases, as in BDD, while using the modelling techniques and automatic test generation algorithms from MBT. We introduce PICKLES, a Precise Input and Control-flow Keyword-based Language for tEst Scenarios; an extension of Gherkin-style BDD scenarios, specified in structured natural language. We provide a bi-directional translation from Pickles scenarios to formal models, where both specifications and tests are human-readable, and a method to obtain a so-called master model combining all translated scenarios. Standard MBT algorithms can then be applied to automatically derive test cases from it. We implement a prototype of the translation and composition steps, which we use on an industrial case study: a software component from a traffic management system. With it, we illustrate the pipeline and show how Pickles can achieve significantly higher coverage with respect to BDD from the same set of scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《PICKLES: a Natural Language Framework for Requirement Specification and Model-Based Testing》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保留行为驱动开发（BDD）中自然语言规格说明的**可读性**的同时，引入基于模型测试（MBT）的**严谨性与自动化生成能力**？
*   **研究背景**：
    *   **MBT 的痛点**：虽然能自动生成高覆盖率测试，但形式化建模门槛高，非技术利益相关者难以理解和维护。
    *   **BDD 的局限**：虽然易读且利于协作，但基于示例（Example-based）的测试往往覆盖率不足，且随着系统复杂度增加，手动维护大量场景的成本极高。
    *   **工业需求**：关键系统需要既能作为文档沟通，又能进行系统化、高覆盖率自动化测试的工具。

### 2. 方法论：PICKLES 框架
*   **核心思想**：引入一种名为 **PICKLES** 的领域特定语言（DSL），它扩展了 Gherkin 语法（Given-When-Then），通过增加变量域定义和约束控制流，实现自然语言与形式化模型（符号迁移系统，STS）之间的双向映射。
*   **关键技术细节**：
    1.  **PicklesDSL 设计**：在传统 BDD 基础上增加了 `Variable Settings` 块，用于定义变量类型（原始类型、数组、结构体）及其取值范围（Range）。
    2.  **双向转换机制**：
        *   **正向**：将 Pickles 场景转换为符号迁移系统（STS）。`Given` 映射为前置条件/初始状态，`When` 映射为输入迁移，`Then` 映射为输出迁移。
        *   **反向**：将 MBT 算法生成的形式化测试路径转换回人类可读的 Pickles 格式，便于非技术人员审计。
    3.  **主模型（Master Model）合成**：定义了“选择组合（Choice Composition）”和“顺序组合（Sequential Composition）”算子，将多个孤立的场景片段融合成一个完整的系统行为模型。
    4.  **测试生成**：利用现有的 MBT 算法（如基于覆盖率的算法）从主模型中自动推导出测试序列。

### 3. 实验设计与 Benchmark
*   **实验场景**：使用了一个真实的工业案例——荷兰 Technolution 公司的**交通管理系统软件组件**。该组件负责处理传感器遥测数据并检测异常交通事件（如低速或静止车辆）。
*   **对比基准（Benchmark）**：
    *   **传统 BDD 方法**：即原始的手动编写、孤立执行的 Cucumber 测试场景。
    *   **对比维度**：迁移覆盖率（Transition Coverage）和输入空间覆盖率（Input Coverage）。
*   **对比方法**：对比了在相同初始场景集下，直接运行 BDD 场景与通过 PICKLES 合成模型后生成测试的效果。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或大规模算力消耗。
*   **实现细节**：原型工具使用 Python 开发，利用 `Lark` 库进行 EBNF 语法解析。由于该方法主要涉及符号逻辑运算和 SMT 求解（用于路径约束），其计算开销通常远低于深度学习模型，普通的办公级 CPU 即可运行。

### 5. 实验数量与充分性
*   **实验规模**：基于 4 个核心业务场景（涉及故障检测器在关键路段内外的各种组合情况）进行了深入分析。
*   **充分性评价**：
    *   **定性分析充分**：详细展示了从自然语言到 STS 模型再到测试用例的完整链路。
    *   **定量对比客观**：通过数学计算展示了覆盖率的提升（从 30% 提升至 100% 的迁移覆盖率）。
    *   **局限性**：实验仅针对单一工业案例，缺乏多行业、大规模复杂系统的横向对比，实验样本量（场景数）较小。

### 6. 主要结论与发现
*   **覆盖率显著提升**：在不增加手动编写场景数量的前提下，通过模型合成，迁移覆盖率提升了 **70%**，输入空间覆盖率从 1.5% 提升至近乎全面覆盖。
*   **发现长序列缺陷**：通过顺序组合场景，PICKLES 能够生成比手动 BDD 更长的操作序列，从而发现跨场景交互产生的潜在 Bug。
*   **降低维护成本**：只需维护少量的通用场景和变量定义，即可自动更新大量底层测试用例。

### 7. 优点与亮点
*   **双向透明性**：测试用例以自然语言返回，解决了 MBT “黑盒”结果难以解释的问题。
*   **语法扩展巧妙**：在保持 Gherkin 风格的同时，通过结构化约束解决了自然语言的歧义性。
*   **工业实用性**：直接兼容现有的 BDD 工具链（如 Cucumber），降低了企业采纳 MBT 技术的门槛。

### 8. 不足与局限
*   **手动适配工作**：虽然测试生成是自动的，但将生成的关键字连接到实际代码的“测试适配器（Test Adapters）”仍需手动编写。
*   **模型爆炸风险**：对于极其复杂的系统，主模型的合成可能会面临状态空间爆炸问题，文中未讨论大规模模型下的性能表现。
*   **非确定性处理**：目前的 DSL 对非确定性行为和复杂控制流（如循环、并行）的支持尚处于初级阶段。
*   **迁移成本**：从现有自由格式的自然语言需求迁移到结构化的 PicklesDSL 仍需要一定的人工重构努力。

（完）
