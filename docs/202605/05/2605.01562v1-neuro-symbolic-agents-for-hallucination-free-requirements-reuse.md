---
title: Neuro-Symbolic Agents for Hallucination-Free Requirements Reuse
title_zh: 用于无幻觉需求复用的神经符号智能体
authors: Ahmed Ibrahim
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01562v1"
tags: ["query:re-us"]
score: 8.0
evidence: 需求编写、管理和模型驱动的启发
tldr: "针对需求重用中传统方法过于僵化以及大语言模型易产生幻觉的问题，本文提出了一种神经符号多智能体系统。该系统将需求重用重新定义为模型驱动的启发式过程，利用LLM在确定的领域模型中进行导航，并结合符号验证器强制执行结构约束。实验表明，该方法在保证100%需求覆盖率的同时，几乎消除了幻觉组合，为受监管行业的合规性提供了透明且有效的解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01562v1/fig-001.webp\", \"caption\": \"Fig. 1. Sub-graph of the eRecordKeeping lattice. Core nodes (mandatory) are in blue; single adaptor node (choose exactly one) is in orange; its children are alternatives (grey); the option node is in green. The deterministic validator enforces these types at runtime.\", \"page\": 3, \"index\": 1, \"width\": 529, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2605.01562v1/fig-002.webp\", \"caption\": \"Fig. 2. LangGraph agent workflow. The Navigator suggests the next discriminant node; the Interpreter (LLM) maps the vision to requirement IDs; the Validator (deterministic Python) checks constraints and may send the proposal back for revision; the Scribe compiles the final specification.\", \"page\": 3, \"index\": 2, \"width\": 529, \"height\": 237}]"
motivation: 传统需求重用框架因匹配规则僵化难以适应多样化语境，而大语言模型虽具灵活性却面临生成无效或不一致需求组合的风险。
method: 提出一种神经符号架构，将LLM作为非确定性启发式工具来遍历确定的需求格模型，并由符号验证器在循环中强制执行所有结构约束。
result: "在自动基准测试中实现了100%的需求覆盖率，约束违反率仅为0.2%，且所有生成的规范在结构上均完全有效。"
conclusion: 该系统通过结合LLM的灵活性与符号逻辑的严谨性，实现了无幻觉的需求重用，并为大规模复杂系统的合规性审计提供了透明路径。
---

## 摘要
面向对象的需求编写与管理方法 (OOMRAM) 是一种需求复用框架，它依赖于精确的标识符匹配和僵化的模板，限制了其在不同语境下调整规范的能力。虽然大语言模型 (LLM) 提供了克服这一瓶颈的灵活性，但它们也带来了生成结构无效或不一致的需求组合的风险。为了解决这一矛盾，我们提出了一种神经符号多智能体系统，将需求复用重新概念化为一种模型驱动的引出过程。在这种范式中，LLM 充当非确定性启发式算法，用于遍历由形式化 OOMRAM 需求格表示的确定性领域模型。确定性的符号验证器在智能体循环中强制执行所有结构约束，从而通过构造有效地消除了幻觉产生的需求组合。在跨越两个应用系列的自主基准测试中，我们的系统实现了 100% 的需求覆盖率，且约束违反率仅为 0.2%。尽管针对单一金标准的 F1 分数处于中等水平 (0.47--0.51)，但生成的每一份规范在结构上都是有效的，并满足所有强制性的领域需求。这种与模型无关的实现通过子图导航可扩展到更大的格，并为监管合规性提供透明的审计追踪。

## Abstract
The Object-Oriented Method for Requirements Authoring and Management (OOMRAM) is a requirements reuse framework that relies on exact identifier matching and rigid templates, limiting its ability to adapt specifications across diverse contexts. While Large Language Models (LLMs) offer the flexibility to overcome this bottleneck, they introduce the risk of generating structurally invalid or inconsistent requirement combinations. To address this tension, we present a neuro-symbolic multi-agent system that re-conceptualizes requirements reuse as a \textbf{Model-Driven Elicitation process}. In this paradigm, an LLM serves as a \textbf{non-deterministic heuristic} for traversing a \textbf{deterministic domain model} represented by a formal OOMRAM requirement lattice. A deterministic, symbolic validator enforces all structural constraints within the agent loop, effectively eliminating hallucinated requirement combinations by construction. Evaluated on an autonomous benchmark across two application families, our system achieves 100\% requirement coverage and a constraint-violation rate of only 0.2\%. Although the F1-score against a single gold standard is moderate (0.47--0.51), every generated specification is structurally valid and satisfies all mandatory domain requirements. The model-agnostic implementation scales to larger lattices via subgraph navigation and provides transparent audit trails for regulatory compliance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Neuro-Symbolic Agents for Hallucination-Free Requirements Reuse》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注软件工程中的**需求复用（Requirements Reuse）**。传统方法如 OOMRAM 依赖精确的标识符匹配，过于僵化，无法处理自然语言描述；而新兴的大语言模型（LLM）虽然灵活，却极易产生“幻觉”，生成逻辑不一致、违反领域约束或虚构标识符的需求组合。
**核心目标**：结合 LLM 的自然语言理解能力与形式化方法的严谨性，构建一个既能理解模糊需求又能保证 100% 结构正确性的需求复用系统。

### 2. 方法论：神经符号多智能体系统
论文提出将需求复用视为**模型驱动的引出（Model-Driven Elicitation）**过程，采用神经符号（Neuro-Symbolic）架构：
*   **核心思想**：将 LLM 作为在确定性“需求格（Lattice）”中导航的非确定性启发式工具，并引入一个纯算法（非 LLM）的符号验证器作为硬性约束闸门。
*   **关键技术细节**：
    *   **需求格模型**：将需求族建模为有向无环图（DAG），定义了核心需求（Core）、单选适配（SA/XOR）、多选适配（MA/OR）和可选需求（Op）等类型。
    *   **多智能体协作（基于 LangGraph）**：
        1.  **Navigator（导航员）**：决定遍历路径。
        2.  **Interpreter（解释员/LLM）**：将用户愿景映射为具体的需求 ID。
        3.  **Validator（验证器/符号逻辑）**：**纯 Python 编写**，根据格约束检查 Interpreter 的输出。若不合规（如 XOR 选了两个），则打回重做。
        4.  **Scribe（记录员）**：生成最终规范。
    *   **子图导航策略**：每次只让 LLM 处理局部节点，确保上下文窗口恒定，从而支持大规模工业级需求格的扩展。

### 3. 实验设计
*   **数据集/场景**：构建了一个包含 12 个领域的语料库，包括 eRecordKeeping（57个需求）、智能家居（38个需求）、汽车信息娱乐（ISO 26262）、核心银行（ISO 20022）、电信 OSS/BSS（49个需求）等。
*   **Benchmark**：针对 10 个项目愿景（Vision）进行自主模拟测试。
*   **对比方法**：
    *   **Gold Standard 对比**：与人类专家手动标注的金标准对比 F1 分数。
    *   **消融实验**：对比了“完整智能体集群”与“纯 LLM 生成（无验证器）”的表现。

### 4. 资源与算力
*   **硬件**：在消费级机器上运行（Apple M1 Pro, 32 GB RAM）。
*   **模型**：使用本地部署的 **Llama 3.1:8B**（通过 Ollama 运行，量化版本 q4_0）。
*   **参数设置**：Temperature 设为 0 以保证确定性和可重复性。
*   **时长**：平均每个愿景的处理延迟约为 210 秒，平均调用 LLM 78 次。

### 5. 实验数量与充分性
*   **实验规模**：详细评估了 3 个具有完整金标准的愿景，并对另外 7 个愿景进行了鲁棒性测试，涵盖了 12 个领域语料库中的代表性部分。
*   **消融实验**：专门设计了移除符号验证器的对比实验。
*   **充分性评价**：实验设计较为客观，通过跨领域（安全关键、高度监管、大规模基础设施）的测试证明了系统的通用性。虽然样本量（10个愿景）不算巨大，但覆盖了复杂的逻辑约束场景，足以验证“消除幻觉”的核心论点。

### 6. 主要结论与发现
*   **零幻觉**：通过符号验证器的强制执行，最终生成的规范实现了 **100% 的结构有效性**，约束违反率降至 0.2%（且在循环中被即时修正）。
*   **有效覆盖**：实现了 100% 的需求覆盖率。
*   **F1 分数的误导性**：F1 分数仅为 0.47-0.81。作者发现这是因为复杂产品线存在大量合法的可选组合，偏离金标准并不代表错误，因此提出“结构有效率（SVR）”是更重要的指标。
*   **自校正能力**：记录显示 LLM 常试图忽略安全需求以降低成本，但会被验证器强制修正。

### 7. 优点
*   **架构创新**：成功将 LangGraph 的智能体流与形式化约束检查结合，解决了 LLM 在严谨工程领域的落地痛点。
*   **可扩展性**：子图导航策略解决了长文本处理中的“迷失”问题，使系统能处理数千个节点的工业级模型。
*   **透明度**：提供了完整的审计追踪，符合受监管行业（如医疗、航空）的合规性要求。

### 8. 不足与局限
*   **预处理依赖**：系统假设已经存在结构化的 OOMRAM 需求格，而从非结构化历史文档提取这些格的过程未在本文解决。
*   **延迟问题**：由于采用多轮迭代和局部导航，LLM 调用次数较多（最高达 101 次），实时交互性有待提升。
*   **模型敏感性**：虽然框架模型无关，但 Interpreter 的初始建议质量仍受限于底层 LLM 的推理能力。
*   **评估局限**：目前主要依赖自动基准测试，缺乏真实人类分析师参与的用户研究（如 NASA-TLX 负荷评估）。

（完）
