---
title: "PICKLES: a Natural Language Framework for Requirement Specification and Model-Based Testing"
title_zh: PICKLES：一种用于需求规范和基于模型测试的自然语言框架
authors: "María Belén Rodríguez, Petra van den Bos"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26572v1"
tags: ["query:re-us"]
score: 8.0
evidence: 用于需求规范和验证的Gherkin风格BDD场景
tldr: 本研究提出PICKLES框架，旨在结合行为驱动开发（BDD）的可读性与基于模型测试（MBT）的自动化优势。通过扩展Gherkin语言，PICKLES实现了结构化自然语言场景与形式化模型之间的双向转换，并能将多个场景整合为全局模型以自动生成测试用例。在工业级交通管理系统案例中，该方法显著提升了测试覆盖率，为需求规范与自动化测试提供了高效的集成方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26572v1/fig-001.webp\", \"caption\": \"Figure 3 Smaster. Each line style (dashed, dotted, solid, dash-dotted) denotes transitions from S1–S4. Variables follow the reference in Table 1.\", \"page\": 10, \"index\": 1, \"width\": 1021, \"height\": 549}, {\"url\": \"assets/figures/arxiv/2604.26572v1/fig-002.webp\", \"caption\": \"Figure 2 STS1: Faulty detectors outside the critical section.\", \"page\": 8, \"index\": 2, \"width\": 520, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.26572v1/fig-003.webp\", \"caption\": \"Figure 4 Coverage of STSmaster. Transitions and states covered by only executing the specification scenarios are shown in black, whereas transitions in red show non-tested transitions.\", \"page\": 12, \"index\": 3, \"width\": 509, \"height\": 367}]"
motivation: 旨在解决BDD在自动化测试生成能力上的局限性，同时保留其自然语言规范的高可读性。
method: 引入PICKLES语言扩展Gherkin场景，并提供与形式化模型之间的双向转换及场景整合为全局模型的算法。
result: 通过工业级交通管理系统的案例研究，证明了PICKLES在相同场景集下能比传统BDD实现显著更高的测试覆盖率。
conclusion: PICKLES成功弥合了自然语言需求描述与自动化模型测试之间的鸿沟，有效提升了软件测试的自动化程度和质量。
---

## 摘要
本文结合了基于模型测试（MBT）和行为驱动开发（BDD）领域的方法，定义了一种测试方法，该方法既具有 BDD 中人类可读的规范和测试用例，又利用了 MBT 的建模技术和自动测试生成算法。我们引入了 PICKLES（一种用于测试场景的精确输入和控制流关键字语言），它是对 Gherkin 风格 BDD 场景的扩展，采用结构化自然语言进行规范。我们提供了从 Pickles 场景到形式化模型的双向转换，其中规范和测试均是人类可读的，并提供了一种获取所谓“主模型”（master model）的方法，该模型结合了所有转换后的场景。随后可以应用标准的 MBT 算法从中自动导出测试用例。我们实现了转换和组合步骤的原型，并将其应用于一个工业案例研究：交通管理系统中的一个软件组件。通过该案例，我们展示了整个流程，并证明了 Pickles 如何在相同场景集下实现比 BDD 显著更高的覆盖率。

## Abstract
This paper combines methods from the fields of Model-Based Testing (MBT) and Behaviour-Driven Development (BDD) to define a testing approach with human-readable specifications and test cases, as in BDD, while using the modelling techniques and automatic test generation algorithms from MBT. We introduce PICKLES, a Precise Input and Control-flow Keyword-based Language for tEst Scenarios; an extension of Gherkin-style BDD scenarios, specified in structured natural language. We provide a bi-directional translation from Pickles scenarios to formal models, where both specifications and tests are human-readable, and a method to obtain a so-called master model combining all translated scenarios. Standard MBT algorithms can then be applied to automatically derive test cases from it. We implement a prototype of the translation and composition steps, which we use on an industrial case study: a software component from a traffic management system. With it, we illustrate the pipeline and show how Pickles can achieve significantly higher coverage with respect to BDD from the same set of scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《PICKLES: a Natural Language Framework for Requirement Specification and Model-Based Testing》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：在关键软件系统中，测试至关重要。**基于模型测试 (MBT)** 能自动生成高覆盖率的测试用例，但因其形式化模型难以编写和维护，在工业界普及受阻。**行为驱动开发 (BDD)** 使用 Gherkin 等自然语言（Given-When-Then）编写场景，易于沟通但缺乏严谨性，且难以处理复杂的控制流和自动生成大规模测试。
*   **核心问题**：如何弥合自然语言需求的可读性与形式化模型测试的严谨性之间的鸿沟？
*   **整体含义**：论文提出了 **PICKLES** 框架，通过扩展 BDD 语法并引入双向转换机制，让非技术利益相关者能用自然语言定义需求，同时享受 MBT 带来的自动化高覆盖率测试。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将结构化的自然语言场景（PicklesDSL）自动转换为形式化的**符号迁移系统 (Symbolic Transition Systems, STS)**，通过组合多个场景生成“主模型”，再利用 MBT 算法生成测试路径，最后将路径反向翻译回自然语言。
*   **关键技术细节**：
    *   **PicklesDSL**：在 Gherkin 基础上增加了 `Variable Settings` 块，明确定义变量类型（原始类型、数组、结构体）和取值范围（Range），并引入显式的控制流关键字。
    *   **语义映射**：
        *   `Given` 映射为 STS 的初始状态和前置条件（Guard）。
        *   `When` 映射为输入迁移（Input Switch）。
        *   `Then` 映射为输出迁移（Output Switch）。
        *   变量和约束映射为 STS 的参数、位置变量和守卫条件。
    *   **模型组合算法**：
        *   **选择组合 (Choice Composition, ⋏)**：将多个初始场景合并，允许系统从初始状态选择任一场景执行。
        *   **顺序组合 (Sequential Composition, ▷)**：将一个场景的终点与另一个场景的起点连接，支持场景的链式执行。
    *   **双向转换**：不仅能从需求生成模型，还能将 MBT 生成的抽象测试序列（包含具体数值）翻译回人类可读的 Pickles 测试脚本。

### 3. 实验设计
*   **场景/案例研究**：使用了荷兰 Technolution B.V. 公司的一个**工业级交通管理系统组件**作为案例。该组件负责根据传感器数据检测道路异常（如低速或静止车辆），并根据其是否在“关键路段”更新 UI 状态。
*   **Benchmark（基准）**：对比了**传统 BDD 方法**（即仅执行手动编写的原始场景）。
*   **对比维度**：主要对比了**迁移覆盖率 (Transition Coverage)** 和 **输入空间覆盖率 (Input Coverage)**。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件算力（如 GPU/CPU 型号）。
*   **实现工具**：原型工具使用 Python 开发，利用 `Lark` 库进行 EBNF 语法解析，并提到使用 SMT 求解器（用于路径条件求解以生成具体测试值），但未详述求解器的具体运行开销。

### 5. 实验数量与充分性
*   **实验规模**：基于 4 个核心工业场景进行了深入分析。
*   **充分性评价**：
    *   **深度充分**：通过一个复杂的工业案例完整展示了从需求到模型、再到组合模型、最后生成测试的全流程。
    *   **广度有限**：目前仅在一个特定领域的工业案例上进行了验证，缺乏多领域、大规模项目集的横向对比。
    *   **客观性**：实验对比了手动场景与自动生成场景的覆盖率差异，数据支撑清晰，逻辑严密。

### 6. 主要结论与发现
*   **覆盖率大幅提升**：在相同的初始场景集下，PICKLES 生成的测试用例比传统 BDD 提升了 **70% 的迁移覆盖率**（从 30% 提升到 100%）。
*   **输入空间扩展**：通过参数化定义，PICKLES 能覆盖 98.5% 的潜在输入组合，而传统 BDD 仅能覆盖约 1.5% 的硬编码示例。
*   **维护成本降低**：只需维护少量的自然语言场景，即可自动生成覆盖各种边界条件和交互序列的复杂测试。

### 7. 优点
*   **易用性与严谨性平衡**：保留了 BDD 的人类可读性，同时引入了 STS 的形式化威力。
*   **双向透明**：测试结果以自然语言反馈，方便非技术人员理解测试失败的原因。
*   **自动化组合**：能够自动发现场景间的交互逻辑，识别出手动编写测试时难以察觉的路径。

### 8. 不足与局限
*   **迁移成本**：虽然比直接写形式化模型简单，但从普通 Gherkin 迁移到 PICKLES 仍需手动定义变量域和约束，存在一定的学习和适配成本。
*   **状态爆炸风险**：在极其复杂的系统中，场景组合生成的“主模型”可能面临状态空间爆炸问题，论文未深入讨论其在大规模系统中的性能表现。
*   **适配器依赖**：与 BDD 一样，最终执行测试仍需手动编写底层代码适配器（Step Definitions）。
*   **非确定性处理**：目前的 DSL 对非确定性行为的支持尚在计划中，目前主要处理确定性逻辑。

（完）
