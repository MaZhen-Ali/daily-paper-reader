---
title: "GLiNER-Relex: A Unified Framework for Joint Named Entity Recognition and Relation Extraction"
title_zh: GLiNER-Relex：一种用于联合命名实体识别与关系抽取的统一框架
authors: "Ihor Stepanov, Oleksandr Lukashov, Mykhailo Shtopko, Vivek Kalyanarangan"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10108v1"
tags: ["query:re-us"]
score: 6.0
evidence: 从非结构化文本中提取信息以进行知识构建
tldr: GLiNER-Relex 是一个将命名实体识别（NER）与关系抽取（RE）统一在单个模型中的框架，扩展了 GLiNER 架构。它利用共享的双向 Transformer 编码器同时表示文本、实体和关系标签，支持在推理阶段进行任意类型的零样本提取。该模型在多个标准基准测试中表现优异，兼顾了计算效率与灵活性，并提供了易用的开源 API。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10108v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the GLiNER-Relex architecture.\", \"page\": 5, \"index\": 1, \"width\": 786, \"height\": 1023}, {\"url\": \"assets/figures/arxiv/2605.10108v1/fig-002.webp\", \"caption\": \"Table 1: Hyperparameters for the released GLiNER-Relex checkpoint.\", \"page\": 10, \"index\": 2, \"width\": 764, \"height\": 744}, {\"url\": \"assets/figures/arxiv/2605.10108v1/fig-003.webp\", \"caption\": \"Table 2: Zero-shot relation extraction performance (Micro-F1 %) on four benchmarks. Bold indicates the best performance per dataset across end-to-end systems. The “Entities” column indicates how each model obtains entity spans at inference time: predicted (joint extraction from raw text), prompted (LLM extracts entities and relations jointly from natural-language prompt), or gold (ground-truth entities supplied as input). The “Avg.” column reports the unweighted mean Micro-F1 across the four benchmarks. †GLiREL is a dedicated relation classifier evaluated with ground-truth entity spans and types, and is not directly comparable to the end-to-end systems; we include it as an upper-bound reference for specialized relation classification.\", \"page\": 12, \"index\": 3, \"width\": 936, \"height\": 180}]"
motivation: 旨在解决现有 NER 和 RE 任务通常需要独立模型且难以灵活处理未知类别的问题，实现统一且支持零样本的联合抽取。
method: 通过共享编码器对文本和标签进行联合建模，并利用识别出的实体跨度构建实体对表示，与关系类型嵌入进行匹配评分。
result: 在 CoNLL04、DocRED、FewRel 和 CrossRE 等数据集上展现出与专用模型及大语言模型相当的竞争力。
conclusion: GLiNER-Relex 证明了统一架构在高效、灵活的实体关系联合抽取任务中的有效性，为知识图谱构建提供了实用的开源方案。
---

## 摘要
联合命名实体识别（NER）与关系抽取（RE）是自然语言处理中从非结构化文本构建知识图谱的一项基础任务。虽然近期的方法将 NER 和 RE 视为需要不同模型的独立任务，但我们引入了 GLiNER-Relex，这是一种扩展了 GLiNER 框架的统一架构，可在单个模型中同时执行实体识别和关系抽取。我们的方法利用共享的双向 Transformer 编码器来联合表示文本、实体类型标签和关系类型标签，从而实现对推理时指定的任意实体和关系类型的零样本（zero-shot）抽取。GLiNER-Relex 从识别出的跨度（spans）构建实体对表示，并使用专门的关系评分模块根据关系类型嵌入对其进行评分。我们在四个标准关系抽取基准数据集（CoNLL04、DocRED、FewRel 和 CrossRE）上评估了我们的模型，并证明了其与专门的关系抽取模型和大型语言模型相比具有竞争力的性能，同时保持了 GLiNER 系列特有的计算效率。该模型以开源 Python 包的形式发布，具有简单的推理 API，允许用户在推理时指定任意实体和关系类型标签，并在单次调用中获取实体和关系三元组。所有模型和代码均已公开。

## Abstract
Joint named entity recognition (NER) and relation extraction (RE) is a fundamental task in natural language processing for constructing knowledge graphs from unstructured text. While recent approaches treat NER and RE as separate tasks requiring distinct models, we introduce GLiNER-Relex, a unified architecture that extends the GLiNER framework to perform both entity recognition and relation extraction in a single model. Our approach leverages a shared bidirectional transformer encoder to jointly represent text, entity type labels, and relation type labels, enabling zero-shot extraction of arbitrary entity and relation types specified at inference time. GLiNER-Relex constructs entity pair representations from recognized spans and scores them against relation type embeddings using a dedicated relation scoring module. We evaluate our model on four standard relation extraction benchmarks: CoNLL04, DocRED, FewRel, and CrossRE, and demonstrate competitive performance against both specialized relation extraction models and large language models, while maintaining the computational efficiency characteristic of the GLiNER family. The model is released as an open-source Python package with a simple inference API that allows users to specify arbitrary entity and relation type labels at inference time and obtain both entities and relation triplets in a single call. All models and code are publicly available.