---
title: "PICKLES: a Natural Language Framework for Requirement Specification and Model-Based Testing"
title_zh: PICKLES：一种用于需求规范和基于模型测试的自然语言框架
authors: "María Belén Rodríguez, Petra van den Bos"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26572v1"
tags: ["query:re-us"]
score: 8.0
evidence: 用于需求规范和行为驱动开发场景的自然语言框架
tldr: 本研究结合了基于模型测试（MBT）和行为驱动开发（BDD）的优势，提出了PICKLES框架。该框架引入了一种扩展自Gherkin的结构化自然语言，用于编写可读性强的需求规范。通过将PICKLES场景双向转换为形式化模型并构建统一的主模型，利用标准MBT算法自动生成测试用例。在工业级交通管理系统案例中，该方法在保持高可读性的同时，显著提升了测试覆盖率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26572v1/fig-001.webp\", \"caption\": \"Figure 3 Smaster. Each line style (dashed, dotted, solid, dash-dotted) denotes transitions from S1–S4. Variables follow the reference in Table 1.\", \"page\": 10, \"index\": 1, \"width\": 1021, \"height\": 549}, {\"url\": \"assets/figures/arxiv/2604.26572v1/fig-002.webp\", \"caption\": \"Figure 2 STS1: Faulty detectors outside the critical section.\", \"page\": 8, \"index\": 2, \"width\": 520, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.26572v1/fig-003.webp\", \"caption\": \"Figure 4 Coverage of STSmaster. Transitions and states covered by only executing the specification scenarios are shown in black, whereas transitions in red show non-tested transitions.\", \"page\": 12, \"index\": 3, \"width\": 509, \"height\": 367}]"
motivation: 旨在结合BDD的人类可读性与MBT的自动化测试生成能力，解决传统BDD方法在测试覆盖率和自动化建模方面的局限性。
method: 开发了PICKLES语言及其与形式化模型的双向转换机制，通过整合多个场景构建主模型，并应用MBT算法自动派生测试用例。
result: 原型实现和工业案例研究表明，在相同场景集下，PICKLES比传统BDD方法能实现显著更高的测试覆盖率。
conclusion: PICKLES成功弥合了自然语言需求规范与形式化模型测试之间的鸿沟，为提升软件测试的自动化程度和质量提供了有效方案。
---

## 摘要
本文结合了基于模型测试（MBT）和行为驱动开发（BDD）领域的方法，定义了一种测试方法，该方法如 BDD 一样具有人类可读的规范和测试用例，同时利用了 MBT 的建模技术和自动测试生成算法。我们引入了 PICKLES（一种用于测试场景的精确输入和控制流关键字语言）；它是 Gherkin 风格 BDD 场景的扩展，采用结构化自然语言进行描述。我们提供了 Pickles 场景与形式化模型之间的双向转换，使规范和测试均保持人类可读，并提出了一种获取所谓“主模型”的方法，该模型整合了所有转换后的场景。随后可应用标准 MBT 算法从中自动导出测试用例。我们实现了转换和组合步骤的原型，并将其应用于一个工业案例研究：某交通管理系统的软件组件。通过该案例，我们展示了完整流程，并证明了 Pickles 如何在相同场景集下实现比 BDD 显著更高的覆盖率。

## Abstract
This paper combines methods from the fields of Model-Based Testing (MBT) and Behaviour-Driven Development (BDD) to define a testing approach with human-readable specifications and test cases, as in BDD, while using the modelling techniques and automatic test generation algorithms from MBT. We introduce PICKLES, a Precise Input and Control-flow Keyword-based Language for tEst Scenarios; an extension of Gherkin-style BDD scenarios, specified in structured natural language. We provide a bi-directional translation from Pickles scenarios to formal models, where both specifications and tests are human-readable, and a method to obtain a so-called master model combining all translated scenarios. Standard MBT algorithms can then be applied to automatically derive test cases from it. We implement a prototype of the translation and composition steps, which we use on an industrial case study: a software component from a traffic management system. With it, we illustrate the pipeline and show how Pickles can achieve significantly higher coverage with respect to BDD from the same set of scenarios.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：PICKLES 框架

#### 1. 论文的核心问题与整体含义（研究动机和背景）
论文针对软件测试中**基于模型测试（MBT）**与**行为驱动开发（BDD）**之间的鸿沟提出了解决方案。
*   **研究动机**：在关键系统中，MBT 虽能通过形式化模型实现系统化、高覆盖率的测试，但因其建模门槛高、可读性差，在工业界普及受阻。相比之下，BDD（如 Gherkin 语言）因其自然语言形式易于沟通，但其基于示例（Example-based）的特性导致测试套件难以维护，且容易出现歧义、不一致和覆盖率不足的问题。
*   **核心问题**：如何结合 BDD 的可读性与 MBT 的严谨性，使非技术利益相关者也能参与形式化建模，并自动生成高覆盖率的人类可读测试用例？

#### 2. 论文提出的方法论
论文引入了 **PICKLES** 框架，其核心思想是将结构化自然语言场景转化为形式化模型，进行组合后再生成测试。
*   **PicklesDSL**：对 Gherkin 语法的扩展，增加了变量域定义（Variable Settings）、约束（Guard Blocks）和控制流描述。
*   **双向转换机制**：
    *   **正向**：将 Pickles 场景映射为**符号迁移系统（STS）**。Given 映射为前置条件/初始状态，When 映射为输入迁移，Then 映射为输出迁移。
    *   **逆向**：将 MBT 算法生成的符号测试用例转换回 Pickles 格式，确保测试结果对人类透明。
*   **主模型组合（Master Model Composition）**：
    *   **选择组合（Choice）**：合并多个场景的初始位置，允许系统从初始状态选择执行任一场景。
    *   **顺序组合（Sequential）**：将一个场景的终点与另一个场景的起点连接，模拟连续的业务流程。
*   **算法流程**：Pickles 场景 $\rightarrow$ 多个局部 STS $\rightarrow$ 组合成主模型 $\rightarrow$ 应用标准 MBT 算法（如覆盖率驱动算法）生成测试路径 $\rightarrow$ 翻译回 Pickles 测试用例。

#### 3. 实验设计
*   **场景与数据集**：使用了荷兰 Technolution B.V. 公司的工业案例——**交通管理系统的一个软件组件**。该组件负责处理道路传感器遥测数据，识别关键路段的异常事件（如车辆停滞）。
*   **Benchmark（基准）**：该公司现有的、基于 Cucumber 实现的传统 BDD 测试套件。
*   **对比方法**：对比了“传统 BDD（孤立执行场景）”与“PICKLES（组合场景并参数化执行）”在**迁移覆盖率（Transition Coverage）**和**输入覆盖率（Input Coverage）**上的表现。

#### 4. 资源与算力
*   论文**未明确说明**具体的算力资源（如 GPU 型号或训练时长）。
*   由于该框架基于符号逻辑和解析技术（使用 Python 的 Lark 库实现原型），而非深度学习模型，其运行开销主要集中在 SMT 求解器（用于路径约束求解）和模型组合上，通常在普通工作站即可完成，无需大规模 GPU 算力。

#### 5. 实验数量与充分性
*   **实验规模**：主要围绕一个深入的工业案例研究展开，包含 4 个核心业务场景及其参数化扩展。
*   **充分性评价**：
    *   **客观性**：实验基于真实的工业需求和现有的 BDD 脚本，具有较强的现实意义。
    *   **局限性**：实验数量相对较少（仅一个案例研究），虽然展示了覆盖率从 30% 提升到 100% 的显著效果，但尚未在更多样化、更大规模的系统上进行压力测试。
    *   **公平性**：对比是在相同的初始场景集下进行的，展示了 PICKLES 通过模型组合挖掘潜在路径的能力。

#### 6. 论文的主要结论与发现
*   **覆盖率显著提升**：在相同的手写场景基础上，PICKLES 将迁移覆盖率提升了 **70%**（从 30% 提升至 100%），输入覆盖率提升了 **98.5%**。
*   **降低维护成本**：通过参数化和模型组合，少量 Pickles 场景即可代表成百上千个具体的测试用例，减少了手动编写重复示例的工作量。
*   **增强可读性**：生成的测试用例保持了自然语言格式，方便非技术人员理解测试路径和失败原因。

#### 7. 优点
*   **双向透明性**：不仅能从自然语言生成模型，还能将复杂的测试路径还原为人类可读的文档。
*   **无缝集成**：生成的测试用例兼容现有的 BDD 工具（如 Cucumber、Robot Framework），降低了企业迁移成本。
*   **形式化严谨性**：引入了变量域和约束检查，能在建模阶段发现需求中的歧义和不一致。

#### 8. 不足与局限
*   **适配器开发**：虽然测试用例是自动生成的，但将测试步骤连接到实际代码的“步骤定义（Step Definitions）”仍需手动编写。
*   **表达力限制**：目前的 DSL 对非确定性行为和复杂控制流（如循环、并发）的支持尚在初期阶段。
*   **案例单一**：目前的结论高度依赖于单个交通系统的案例，其在分布式系统或超大规模逻辑系统中的表现有待验证。
*   **迁移门槛**：虽然比纯形式化方法简单，但从普通自然语言转向“结构化”PicklesDSL 仍需要一定的学习成本和对变量域的精确定义。

（完）
